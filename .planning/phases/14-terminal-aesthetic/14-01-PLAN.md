---
phase: 14-terminal-aesthetic
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [package.json, src/cli/theme/theme.test.ts, src/cli/theme/detection.test.ts, src/cli/theme/colors.test.ts, src/cli/theme/borders.test.ts]
autonomous: true
requirements: [UI-01, UI-03, UI-04, UI-05, UI-06]
user_setup: []
must_haves:
  truths:
    - "picocolors installed and chalk removed from dependencies"
    - "Test scaffolds exist for all theme module components"
    - "Theme directory structure created"
  artifacts:
    - path: "package.json"
      provides: "Dependency configuration"
      contains: '"picocolors"'
      not_contains: '"chalk"'
    - path: "src/cli/theme/theme.test.ts"
      provides: "Test scaffold for theme module"
      contains: "describe('theme module')"
    - path: "src/cli/theme/detection.test.ts"
      provides: "Test scaffold for detection module"
      contains: "describe('detectColorSupport')"
    - path: "src/cli/theme/colors.test.ts"
      provides: "Test scaffold for colors module"
      contains: "describe('colors')"
    - path: "src/cli/theme/borders.test.ts"
      provides: "Test scaffold for borders module"
      contains: "describe('getBorders')"
---

<objective>
Wave 0: Establish theme module foundation — install picocolors dependency, uninstall chalk, create test scaffolds for all theme components, and set up theme directory structure.

Purpose: Per D-01, picocolors is the chosen ANSI library (zero-dependency, fastest, smallest). Test scaffolds ensure TDD coverage from Wave 1 onward. Theme directory provides unified design system location.

Output: Updated package.json with picocolors, test file stubs, theme directory created.
</objective>

<execution_context>
@/Users/lihaoxuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lihaoxuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-terminal-aesthetic/14-CONTEXT.md
@.planning/phases/14-terminal-aesthetic/14-RESEARCH.md
@.planning/phases/14-terminal-aesthetic/14-UI-SPEC.md
</context>

<interfaces>
<!-- Key interfaces executor needs. No codebase exploration required. -->

From RESEARCH.md:
```typescript
// Test framework: Vitest (already in project)
// Quick run: vitest run src/cli/theme/*.test.ts

// Color Support interface (detection.ts will implement):
export interface ColorSupport {
  enabled: boolean;    // ANSI colors supported
  truecolor: boolean;  // 24-bit truecolor supported
  reason: string;      // Detection reason
}

// OpenCode palette ANSI codes (colors.ts will use):
const OPENCODE = {
  darkBg:  '\x1b[48;2;32;29;29m',   // #201d1d background
  lightFg: '\x1b[38;2;253;252;252m', // #fdfcfc foreground
  muted:   '\x1b[38;2;154;152;152m', // #9a9898 muted
  reset:   '\x1b[0m',
};

// Border characters (borders.ts will implement):
export const BORDERS = {
  unicode: {
    horizontal: '─',  // U+2500
    vertical:   '│',  // U+2502
    topLeft:    '┌',  // U+250C
    topRight:   '┐',  // U+2510
    bottomLeft: '└',  // U+2514
    bottomRight: '┘', // U+2518
    cross:      '┼',  // U+253C
  },
  ascii: {
    horizontal: '-',
    vertical:   '|',
    topLeft:    '+',
    topRight:   '+',
    bottomLeft: '+',
    bottomRight: '+',
    cross:      '+',
  },
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install picocolors and uninstall chalk</name>
  <files>package.json, package-lock.json (if exists)</files>
  <read_first>
    - /Users/lihaoxuan/code/P07_CCAPISwitch/package.json (current dependencies)
  </read_first>
  <action>
    Per D-01: Replace chalk with picocolors as the ANSI color library.

    Execute the following commands:
    1. `npm uninstall chalk` — Remove chalk from dependencies
    2. `npm install picocolors@1.1.1` — Install picocolors (exact version per RESEARCH.md)

    After installation, verify package.json contains:
    - `"picocolors": "1.1.1"` in dependencies
    - No `"chalk"` entry in dependencies

    Do NOT modify any source files yet — only dependency changes.
  </action>
  <verify>
    <automated>grep -q '"picocolors"' package.json && ! grep -q '"chalk"' package.json && echo "PASS"</automated>
  </verify>
  <done>
    package.json contains `"picocolors": "1.1.1"` (or similar version range)
    package.json does NOT contain `"chalk"` entry
    npm install completed successfully
  </done>
  <acceptance_criteria>
    - package.json contains line matching `"picocolors":`
    - package.json does NOT contain line matching `"chalk":`
    - `npm ls picocolors` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create theme directory and test scaffolds</name>
  <files>src/cli/theme/theme.test.ts, src/cli/theme/detection.test.ts, src/cli/theme/colors.test.ts, src/cli/theme/borders.test.ts</files>
  <read_first>
    - /Users/lihaoxuan/code/P07_CCAPISwitch/vitest.config.ts (test framework configuration)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/cli/output/table.ts (existing test patterns reference)
  </read_first>
  <action>
    Create the theme module directory structure and test scaffold files.

    1. Create directory: `src/cli/theme/`

    2. Create test scaffolds with describe blocks and placeholder tests:

    **src/cli/theme/theme.test.ts:**
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('theme module', () => {
      it('placeholder: colors are defined', () => {
        // Wave 1 will implement
        expect(true).toBe(true);
      });

      it('placeholder: formatters work', () => {
        // Wave 2 will implement
        expect(true).toBe(true);
      });
    });
    ```

    **src/cli/theme/detection.test.ts:**
    ```typescript
    import { describe, it, expect, vi } from 'vitest';

    describe('detectColorSupport', () => {
      it('placeholder: NO_COLOR disables colors', () => {
        // Wave 1 will implement
        expect(true).toBe(true);
      });

      it('placeholder: Windows Terminal detected', () => {
        // Wave 1 will implement
        expect(true).toBe(true);
      });
    });
    ```

    **src/cli/theme/colors.test.ts:**
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('colors', () => {
      it('placeholder: OpenCode palette colors exist', () => {
        // Wave 1 will implement
        expect(true).toBe(true);
      });

      it('placeholder: semantic colors exist', () => {
        // Wave 1 will implement
        expect(true).toBe(true);
      });
    });
    ```

    **src/cli/theme/borders.test.ts:**
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('getBorders', () => {
      it('placeholder: Unicode borders for modern terminals', () => {
        // Wave 1 will implement
        expect(true).toBe(true);
      });

      it('placeholder: ASCII fallback for Windows CMD', () => {
        // Wave 1 will implement
        expect(true).toBe(true);
      });
    });
    ```

    Each test file must:
    - Import from 'vitest'
    - Have describe block matching the module name
    - Have at least 2 placeholder it blocks
    - Pass when run: `vitest run src/cli/theme/*.test.ts`
  </action>
  <verify>
    <automated>vitest run src/cli/theme/*.test.ts 2>&1 | grep -q "passed" && echo "PASS"</automated>
  </verify>
  <done>
    src/cli/theme/ directory exists
    All 4 test files exist with describe blocks
    All tests pass (placeholder tests should pass)
  </done>
  <acceptance_criteria>
    - `ls src/cli/theme/` shows 4 .test.ts files
    - `vitest run src/cli/theme/*.test.ts` exits 0
    - Each test file contains `describe(` block
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package.json | External dependency installation (picocolors) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | package.json | accept | npm audit will verify picocolors integrity; picocolors is well-established (1.1.1) |
| T-14-02 | Spoofing | npm registry | accept | npm registry is trusted source; version pinning prevents typosquatting |

No high/critical threats. Dependency change is low-risk.
</threat_model>

<verification>
- [ ] package.json updated: picocolors in, chalk out
- [ ] Theme directory created: src/cli/theme/
- [ ] Test scaffolds created: 4 test files
- [ ] All placeholder tests pass
</verification>

<success_criteria>
**Wave 0 complete when:**
1. `npm ls picocolors` exits 0 (picocolors installed)
2. `npm ls chalk` exits 1 (chalk removed)
3. `ls src/cli/theme/*.test.ts` shows 4 files
4. `vitest run src/cli/theme/*.test.ts` passes all tests
</success_criteria>

<output>
After completion, create `.planning/phases/14-terminal-aesthetic/14-01-SUMMARY.md`
</output>
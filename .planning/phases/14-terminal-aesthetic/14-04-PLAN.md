---
phase: 14-terminal-aesthetic
plan: 04
type: execute
wave: 3
depends_on: [14-03]
files_modified: [src/cli/output/table.ts, src/cli/output/error.ts, src/cli/utils/diff-render.ts, src/cli/prompts/utils/format-choices.ts, src/cli/index.ts, src/cli/prompts/utils/theme.ts]
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06]
user_setup: []
must_haves:
  truths:
    - "All CLI output modules import from unified theme module"
    - "chalk imports removed from all output files"
    - "Old prompts/utils/theme.ts file removed"
    - "NO_COLOR handling centralized in theme module (not manual in index.ts)"
    - "Table output uses border characters from theme"
    - "Error/diff output uses semantic colors from theme"
  artifacts:
    - path: "src/cli/output/table.ts"
      provides: "Table rendering with theme colors"
      imports: "from '../theme/index.js'"
      not_contains: "import chalk"
    - path: "src/cli/output/error.ts"
      provides: "Error output with theme colors"
      imports: "from '../theme/index.js'"
      not_contains: "import chalk"
    - path: "src/cli/utils/diff-render.ts"
      provides: "Diff output with theme colors"
      imports: "from '../theme/index.js'"
      not_contains: "import chalk"
    - path: "src/cli/prompts/utils/format-choices.ts"
      provides: "Choice formatting with theme colors"
      imports: "from '../../theme/index.js'"
      not_contains: "import chalk"
    - path: "src/cli/index.ts"
      provides: "CLI entry point"
      not_contains: "import chalk"
      not_contains: "if (process.env.NO_COLOR) chalk.level"
    - path: "src/cli/prompts/utils/theme.ts"
      provides: "Old theme file"
      exists: false
  key_links:
    - from: "src/cli/output/table.ts"
      to: "src/cli/theme/index.ts"
      via: "import"
      pattern: "from '../theme/index.js'"
    - from: "src/cli/output/error.ts"
      to: "src/cli/theme/index.ts"
      via: "import"
      pattern: "from '../theme/index.js'"
    - from: "src/cli/utils/diff-render.ts"
      to: "src/cli/theme/index.ts"
      via: "import"
      pattern: "from '../theme/index.js'"
---

<objective>
Wave 3: Integrate theme module into all CLI output consumers — update table.ts, error.ts, diff-render.ts, format-choices.ts, index.ts to use theme module, remove old prompts/utils/theme.ts, remove chalk from all imports.

Purpose: Replace all chalk-based color formatting with unified theme module. This is the integration wave that makes the design system visible to users. Remove manual NO_COLOR handling from index.ts (theme module handles it centrally).

Output: All output files use theme module, chalk completely removed, old theme.ts deleted.
</objective>

<execution_context>
@/Users/lihaoxuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lihaoxuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-terminal-aesthetic/14-CONTEXT.md
@.planning/phases/14-terminal-aesthetic/14-RESEARCH.md
@.planning/phases/14-terminal-aesthetic/14-UI-SPEC.md
@.planning/phases/14-terminal-aesthetic/14-03-SUMMARY.md
</context>

<interfaces>
<!-- Interfaces from Wave 2 -->

From Wave 2 (14-03-SUMMARY):
- src/cli/theme/index.ts exports: colors, borders, formatters, detection
- formatters: message, hint, error, success, warning, separator, cancel

Current consumer files (to be updated):
- src/cli/output/table.ts — uses chalk.cyan, chalk.green, chalk.gray, chalk.yellow
- src/cli/output/error.ts — uses chalk.red
- src/cli/utils/diff-render.ts — uses chalk.red, chalk.green, chalk.yellow, chalk.gray
- src/cli/prompts/utils/format-choices.ts — uses chalk.green, chalk.gray, chalk.red
- src/cli/prompts/utils/theme.ts — uses chalk.hex (to be deleted)
- src/cli/index.ts — imports chalk, has manual NO_COLOR handling
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update table.ts and error.ts to use theme module</name>
  <files>src/cli/output/table.ts, src/cli/output/error.ts</files>
  <read_first>
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/cli/output/table.ts (current chalk usage)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/cli/output/error.ts (current chalk usage)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/cli/theme/index.ts (theme module exports)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/cli/theme/colors.ts (color functions)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/cli/theme/borders.ts (border characters)
  </read_first>
  <action>
    Replace chalk imports with theme module in table.ts and error.ts.

    **table.ts changes:**

    Replace:
    ```typescript
    import chalk from 'chalk';
    ```

    With:
    ```typescript
    import { colors, getBorders } from '../theme/index.js';
    ```

    Replace all chalk calls:
    - `chalk.cyan.bold('Project')` → `colors.accent(colors.bold('Project'))`
    - `chalk.green(project.activeConfig)` → `colors.success(project.activeConfig)`
    - `chalk.gray('none')` → `colors.muted('none')`
    - `chalk.gray(project.path)` → `colors.muted(project.path)`
    - `chalk.yellow('○')` → `colors.warning('○')`
    - `chalk.white(projectName)` → `colors.foreground(projectName)` or just `projectName`

    Update table border style to use theme borders:
    ```typescript
    const borders = getBorders();
    const table = new Table({
      head: [...],
      colWidths: [20, 40, 15, 10],
      style: {
        head: [],
        border: [], // picocolors handles NO_COLOR, no need for ['gray']
      },
    });
    ```

    For "No projects registered" message:
    - `chalk.yellow('No projects registered.')` → `colors.warning('No projects registered.')`

    **error.ts changes:**

    Replace:
    ```typescript
    import chalk from 'chalk';
    ```

    With:
    ```typescript
    import { colors, formatters } from '../theme/index.js';
    ```

    Replace chalk calls:
    - `chalk.red(`[${error.code}] ${error.message}`)` → `formatters.error(`${error.code} ${error.message}`)`
    - `chalk.red(`Error: ${error.message}`)` → `formatters.error(error.message)`
    - `chalk.red('Unknown error occurred')` → `formatters.error('Unknown error occurred')`

    Note: formatters.error() already includes ✗ symbol, so adjust accordingly:
    - For ServiceError: `formatters.error(`[${error.code}] ${error.message}`)` → `colors.danger(`[${error.code}] ${error.message}`)`
    - For generic Error: `colors.danger(`Error: ${error.message}`)`

    Actually, let's use colors.danger directly for error.ts since we need precise formatting:
    ```typescript
    console.error(colors.danger(`[${error.code}] ${error.message}`));
    console.error(colors.danger(`Error: ${error.message}`));
    console.error(colors.danger('Unknown error occurred'));
    ```

    Run tests to verify: `vitest run src/cli/output/`
  </action>
  <verify>
    <automated>grep -L "chalk" src/cli/output/table.ts src/cli/output/error.ts && vitest run src/cli/output/ 2>&1 | grep -E "(passed|Tests)" | head -1</automated>
  </verify>
  <done>
    table.ts imports from '../theme/index.js'
    error.ts imports from '../theme/index.js'
    No chalk imports in table.ts or error.ts
    Table output tests pass
    Colors use semantic names (accent, danger, success, warning, muted)
  </done>
  <acceptance_criteria>
    - table.ts contains `import { colors, getBorders } from '../theme/index.js'`
    - table.ts does NOT contain `import chalk`
    - table.ts contains `colors.accent` or `colors.success` or `colors.muted`
    - error.ts contains `import { colors } from '../theme/index.js'`
    - error.ts does NOT contain `import chalk`
    - error.ts contains `colors.danger`
    - `vitest run src/cli/output/` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update diff-render.ts and format-choices.ts to use theme module</name>
  <files>src/cli/utils/diff-render.ts, src/cli/prompts/utils/format-choices.ts</files>
  <read_first>
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/cli/utils/diff-render.ts (current chalk usage)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/cli/prompts/utils/format-choices.ts (current chalk usage)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/cli/theme/index.ts (theme module exports)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/cli/theme/colors.ts (color functions)
  </read_first>
  <action>
    Replace chalk imports with theme module in diff-render.ts and format-choices.ts.

    **diff-render.ts changes:**

    Replace:
    ```typescript
    import chalk from 'chalk';
    ```

    With:
    ```typescript
    import { colors } from '../theme/index.js';
    ```

    Replace chalk calls (per D-05, D-06 from file comments):
    - `chalk.gray(`--- a/${filePath}`)` → `colors.muted(`--- a/${filePath}`)`
    - `chalk.gray(`+++ b/${filePath}`)` → `colors.muted(`+++ b/${filePath}`)`
    - `chalk.gray('配置无变化。')` → `colors.muted('配置无变化。')`
    - `chalk.red(`- ${line.path}: ...`)` → `colors.danger(`- ${line.path}: ...`)`
    - `chalk.green(`+ ${line.path}: ...`)` → `colors.success(`+ ${line.path}: ...`)`
    - `chalk.yellow(`~ ${line.path}: ...`)` → `colors.warning(`~ ${line.path}: ...`)`

    **format-choices.ts changes:**

    Replace:
    ```typescript
    import chalk from 'chalk';
    ```

    With:
    ```typescript
    import { colors } from '../../theme/index.js';
    ```

    Replace chalk calls:
    - `chalk.green(`[${project.activeConfig}]`)` → `colors.success(`[${project.activeConfig}]`)`
    - `chalk.gray('[未配置]')` → `colors.muted('[未配置]')`
    - `chalk.red('取消')` → `colors.danger('取消')`

    Run tests: `vitest run src/cli/utils/` and `vitest run src/cli/prompts/`
  </action>
  <verify>
    <automated>grep -L "chalk" src/cli/utils/diff-render.ts src/cli/prompts/utils/format-choices.ts && echo "NO_CHALK_PASS"</automated>
  </verify>
  <done>
    diff-render.ts imports from '../theme/index.js'
    format-choices.ts imports from '../../theme/index.js'
    No chalk imports in diff-render.ts or format-choices.ts
    Diff colors match semantics (danger=removed, success=added, warning=modified)
    Format-choices uses semantic colors (success, muted, danger)
  </done>
  <acceptance_criteria>
    - diff-render.ts contains `import { colors } from '../theme/index.js'`
    - diff-render.ts does NOT contain `import chalk`
    - diff-render.ts contains `colors.danger` for removed lines
    - diff-render.ts contains `colors.success` for added lines
    - diff-render.ts contains `colors.muted` for headers
    - format-choices.ts contains `import { colors } from '../../theme/index.js'`
    - format-choices.ts does NOT contain `import chalk`
    - format-choices.ts contains `colors.success`
    - format-choices.ts contains `colors.muted`
    - format-choices.ts contains `colors.danger`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update CLI index.ts and remove old theme.ts</name>
  <files>src/cli/index.ts, src/cli/prompts/utils/theme.ts</files>
  <read_first>
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/cli/index.ts (current chalk import and NO_COLOR handling)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/cli/prompts/utils/theme.ts (file to delete)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/cli/theme/index.ts (new theme module)
  </read_first>
  <action>
    Remove chalk import and manual NO_COLOR handling from index.ts, delete old prompts/utils/theme.ts.

    **index.ts changes:**

    Remove these lines:
    ```typescript
    import chalk from 'chalk';
    // ...
    if (process.env.NO_COLOR) chalk.level = 0;
    ```

    The theme module handles NO_COLOR centrally. No need for manual handling in CLI entry.

    Check if index.ts uses chalk elsewhere — if not, simply remove import.
    If chalk is used elsewhere (unlikely based on reading), replace with theme colors.

    **Delete old theme.ts:**

    Remove file: `src/cli/prompts/utils/theme.ts`

    This file used chalk.hex() which is incompatible with picocolors (D-01).
    The new theme module at `src/cli/theme/` replaces it.

    **Update any files importing from old theme.ts:**

    Check if any files import from `./prompts/utils/theme.js` or `./prompts/utils/theme.ts`:
    ```bash
    grep -r "from.*prompts/utils/theme" src/cli/
    ```

    If found, update those imports to use `../../theme/index.js` instead.

    Run full test suite: `npm test`
  </action>
  <verify>
    <automated>test ! -f src/cli/prompts/utils/theme.ts && echo "FILE_DELETED" && grep -L "chalk" src/cli/index.ts && echo "NO_CHALK_INDEX" && npm test 2>&1 | tail -5 | grep -E "(passed|Tests)"</automated>
  </verify>
  <done>
    index.ts has no chalk import
    index.ts has no manual NO_COLOR handling
    prompts/utils/theme.ts file deleted
    No files import from old theme.ts path
    All tests pass (full suite)
  </done>
  <acceptance_criteria>
    - src/cli/prompts/utils/theme.ts does NOT exist (file deleted)
    - src/cli/index.ts does NOT contain `import chalk`
    - src/cli/index.ts does NOT contain `chalk.level = 0`
    - `npm test` exits 0
    - No import statements reference `prompts/utils/theme`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → table.ts | Project names/paths displayed in table (strip ANSI if needed) |
| User input → diff-render.ts | Config values displayed in diff (strip ANSI if needed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Spoofing | ANSI injection in table.ts | mitigate | Strip ANSI codes from project.path before display (add sanitize step) |
| T-14-07 | Spoofing | ANSI injection in diff-render.ts | mitigate | Strip ANSI codes from values before formatting (add sanitize step) |

**Mitigation implementation:**
Add ANSI stripping utility if user input contains ANSI codes:
```typescript
// In formatters.ts or separate sanitize module:
const stripAnsi = (str: string) => str.replace(/\x1b\[[0-9;]*m/g, '');
```

Apply before calling color functions on user-provided strings.
</threat_model>

<verification>
- [ ] table.ts uses theme module
- [ ] error.ts uses theme module
- [ ] diff-render.ts uses theme module
- [ ] format-choices.ts uses theme module
- [ ] index.ts has no chalk
- [ ] prompts/utils/theme.ts deleted
- [ ] npm test passes
</verification>

<success_criteria>
**Wave 3 complete when:**
1. `grep -r "import chalk" src/cli/` returns NO matches
2. `ls src/cli/prompts/utils/theme.ts` fails (file deleted)
3. `npm test` passes all tests
4. All output files import from `../theme/index.js` or `../../theme/index.js`
5. NO_COLOR works: `NO_COLOR=1 cc-config list` shows no colors
</success_criteria>

<output>
After completion, create `.planning/phases/14-terminal-aesthetic/14-04-SUMMARY.md`
</output>
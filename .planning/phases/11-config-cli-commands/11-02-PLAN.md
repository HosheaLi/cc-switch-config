---
phase: 11-config-cli-commands
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified: [src/cli/index.ts, src/cli/prompts/wizards/config-wizard.ts]
autonomous: true
requirements: [CFG-03]
user_setup: []
---

<objective>
Integrate config commands into CLI program and mark config-wizard.ts as deprecated.

Purpose: Per D-03, config command registered to CLI program. Per D-02, config-wizard.ts marked deprecated for Phase 15 removal.
Output: src/cli/index.ts updated, config-wizard.ts with deprecation JSDoc.
</objective>

<execution_context>
@/Users/lihaoxuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lihaoxuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-config-cli-commands/11-CONTEXT.md
@.planning/phases/11-config-cli-commands/11-RESEARCH.md
@.planning/phases/11-config-cli-commands/11-PATTERNS.md

<interfaces>
<!-- Current CLI entry point structure -->

From src/cli/index.ts:
```typescript
// Current imports (lines 6-16)
import { registerListCommand } from './commands/list.js';
import { registerSwitchCommand } from './commands/switch.js';
import { registerCurrentCommand } from './commands/current.js';
import { registerTemplateCommand } from './commands/template.js';
// ... other commands

// Registration calls (lines 29-42)
registerListCommand(program);
registerSwitchCommand(program);
registerCurrentCommand(program);
registerTemplateCommand(program);
// ... other registrations
```

From src/cli/prompts/wizards/config-wizard.ts:
```typescript
// Current file header - add @deprecated JSDoc after imports
// File will remain functional during deprecation period (Phase 15 removes)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register config command in CLI program</name>
  <files>src/cli/index.ts</files>
  <read_first>
    - src/cli/index.ts (current structure, imports, registration calls)
    - src/cli/commands/config.ts (registerConfigCommand export)
  </read_first>
  <acceptance_criteria>
    - grep -q "registerConfigCommand" src/cli/index.ts
    - grep -q "from './commands/config.js'" src/cli/index.ts
    - npm test passes after integration
  </acceptance_criteria>
  <action>
Update src/cli/index.ts to register config command.

**Per D-03:** Add import and registration call.

1. Add import after existing command imports (around line 10):
```typescript
import { registerConfigCommand } from './commands/config.js';
```

2. Add registration call after template command registration (around line 33):
```typescript
registerConfigCommand(program);
```

Order: config command should be registered near template command for consistency.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    - config command accessible via `cc-config config add/list/remove`
    - config command accessible via `cc-config cfg add/list/remove`
    - All tests pass
    - File committed to git
  </done>
</task>

<task type="auto">
  <name>Task 2: Mark config-wizard.ts as deprecated</name>
  <files>src/cli/prompts/wizards/config-wizard.ts</files>
  <read_first>
    - src/cli/prompts/wizards/config-wizard.ts (current content)
    - RESEARCH.md lines 346-350 (deprecation pattern)
    - CONTEXT.md D-02 (mark deprecated, Phase 15 removes)
  </read_first>
  <acceptance_criteria>
    - grep -q "@deprecated" src/cli/prompts/wizards/config-wizard.ts
    - grep -q "cc-config config add" src/cli/prompts/wizards/config-wizard.ts
    - File still functional (no code removal)
  </acceptance_criteria>
  <action>
Add @deprecated JSDoc annotation to config-wizard.ts.

**Per D-02:** Mark deprecated, Phase 15 removes. Keep file functional during deprecation.

Add JSDoc block at top of file (after existing imports, before any exports):
```typescript
/**
 * @deprecated
 *
 * 此 wizard 已废弃，将在 Phase 15 移除。
 * 请使用 CLI 命令替代：
 * - `cc-config config add` 创建配置
 * - `cc-config config list` 查看配置
 * - `cc-config config remove` 删除配置
 *
 * 迁移说明：
 * - 新 CLI 命令使用 ApiService (Phase 10)
 * - 配置存储为 ApiConfig (unified/granular 模式)
 * - API Key 输入使用 password 类型 (SEC-04)
 *
 * 详见 ROADMAP.md Phase 11。
 */
```

DO NOT remove any existing code - file remains functional for backward compatibility.
  </action>
  <verify>
    <automated>grep "@deprecated" src/cli/prompts/wizards/config-wizard.ts</automated>
  </verify>
  <done>
    - @deprecated JSDoc added
    - Migration guidance documented
    - File still functional
    - File committed to git
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI Program -> Config Command | Commander program invokes config subcommand |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Repudiation | Deprecation notice | accept | @deprecated JSDoc documents migration path |
| T-11-10 | Tampering | Command registration | mitigate | Commander validates subcommand structure |

## Implementation Checklist

- [ ] registerConfigCommand imported from commands/config.js
- [ ] registerConfigCommand called with program argument
- [ ] @deprecated JSDoc added to config-wizard.ts
- [ ] config-wizard.ts remains functional (no code removal)
</threat_model>

<verification>
After task completion:
1. Run `npm test` - all tests pass
2. Run `cc-config config --help` - shows add/list/remove subcommands
3. Verify @deprecated in config-wizard.ts
</verification>

<success_criteria>
- config command accessible via CLI program
- config-wizard.ts shows deprecation warning
- All existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/11-config-cli-commands/11-02-SUMMARY.md`
</output>
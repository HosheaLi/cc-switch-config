---
phase: 11-config-cli-commands
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/config.ts
autonomous: true
gap_closure: true
requirements:
  - CFG-03
must_haves:
  truths:
    - "User can see table headers above data rows in config list output"
    - "Headers match column widths (名称 16 chars, 模型 20 chars, API Key)"
    - "Header styling uses chalk.cyan for consistency with title"
  artifacts:
    - path: "src/cli/commands/config.ts"
      provides: "Header row for config list table"
      contains: "console.log after line 147"
  key_links:
    - from: "config list command"
      to: "table output"
      via: "console.log after separator"
      pattern: "名称.*模型.*API Key"
---

<objective>
Add table header row to config list command output.

Purpose: Users need column headers to understand what each column represents in the config list table.
Output: Modified config.ts with header row between separator and data loop.
</objective>

<execution_context>
@/Users/lihaoxuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lihaoxuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-config-cli-commands/11-HUMAN-UAT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add header row to config list table</name>
  <files>src/cli/commands/config.ts</files>
  <action>
    Per UAT gap diagnosis: config list command prints data rows but has no header row.

    **Current code (lines 145-157):**
    ```typescript
    // D-05: Table format output
    console.log(chalk.cyan('\n可用配置'));
    console.log(chalk.gray('─'.repeat(50)));

    for (const [name, cfg] of Object.entries(configs)) {
      const maskedKey = maskApiKey(cfg.apiKey);
      const modelName = cfg.mode === 'unified'
        ? (cfg.modelName ?? '未设置')
        : 'granular';
      console.log(chalk.white(
        `  ${name.padEnd(16)} ${modelName.padEnd(20)} ${maskedKey}`
      ));
    }
    ```

    **Fix:** Insert header row after line 147 (first separator) before the data loop.

    **Change:**
    1. Add header row with chalk.cyan styling (matches title color)
    2. Column widths: 名称 (16 chars), 模型 (20 chars), API Key
    3. Add second separator line after header to visually separate from data

    **Modified section (lines 145-162):**
    ```typescript
    // D-05: Table format output
    console.log(chalk.cyan('\n可用配置'));
    console.log(chalk.gray('─'.repeat(50)));

    // Header row
    console.log(chalk.cyan(
      `  ${'名称'.padEnd(16)} ${'模型'.padEnd(20)} API Key`
    ));
    console.log(chalk.gray('─'.repeat(50)));

    for (const [name, cfg] of Object.entries(configs)) {
      const maskedKey = maskApiKey(cfg.apiKey); // CFG-04/SEC-01
      const modelName = cfg.mode === 'unified'
        ? (cfg.modelName ?? '未设置')
        : 'granular';
      console.log(chalk.white(
        `  ${name.padEnd(16)} ${modelName.padEnd(20)} ${maskedKey}`
      ));
    }

    console.log(chalk.gray('─'.repeat(50)));
    console.log(chalk.gray(`共 ${names.length} 个配置\n`));
    ```

    **Styling decision:** Use chalk.cyan for header (matches title "可用配置") to distinguish from white data rows.
  </action>
  <verify>
    <automated>npm run build && node dist/cli/index.js config list</automated>
  </verify>
  <done>
    Config list output shows header row with "名称", "模型", "API Key" columns between top separator and data rows.
    Header uses chalk.cyan color, data rows use chalk.white.
    Second separator line appears between header and data rows.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` - build succeeds
2. Run `cc-config config list` with existing configs
3. Verify output structure:
   - Title line: "可用配置" (cyan)
   - Separator line (gray)
   - Header row: "名称          模型                 API Key" (cyan)
   - Separator line (gray)
   - Data rows with padded columns (white)
   - Separator line (gray)
   - Count message (gray)
</verification>

<success_criteria>
- [ ] Header row appears between first separator and data loop
- [ ] Header columns match data row widths (16/20 chars)
- [ ] Header uses chalk.cyan for visual distinction
- [ ] Second separator separates header from data
- [ ] Build succeeds with no errors
- [ ] UAT test 2 passes with table headers visible
</success_criteria>

<output>
After completion, create `.planning/phases/11-config-cli-commands/11-03-SUMMARY.md`
</output>
---
phase: 10
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/lib/security/api-key.ts
  - src/lib/security/api-key.test.ts
  - src/lib/security/index.ts
autonomous: true
requirements:
  - CFG-04
  - SEC-01
user_setup: []
must_haves:
  truths:
    - "User sees API key as '...last4' in all display contexts"
    - "CLI args containing apiKey patterns are rejected with SecurityError"
    - "maskApiKey reuses existing maskToken function"
    - "applyMaskedApiKey utility available for display contexts"
  artifacts:
    - path: "src/lib/security/api-key.ts"
      provides: "maskApiKey, applyMaskedApiKey, validateNoCliApiKey"
      min_lines: 40
      contains: "export function maskApiKey"
    - path: "src/lib/security/api-key.test.ts"
      provides: "API key security tests"
      contains: "describe maskApiKey"
    - path: "src/lib/security/index.ts"
      provides: "Barrel export for security module"
      contains: "export * from './api-key.js'"
  key_links:
    - from: "src/lib/security/api-key.ts"
      to: "src/lib/security/token-check.ts"
      via: "maskToken import"
      pattern: "import { maskToken } from './token-check.js'"
    - from: "validateNoCliApiKey"
      to: "ServiceError"
      via: "throw on CLI args"
      pattern: "throw new ServiceError(..., 'SECURITY_VIOLATION')"
---

<objective>
Create API key security utilities for masking and CLI args enforcement.

Purpose: Mask API key in display contexts (CFG-04) and prevent CLI args exposure (SEC-01).
Output: maskApiKey reusing maskToken, validateNoCliApiKey for service-layer enforcement.
</objective>

<execution_context>
@/Users/lihaoxuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lihaoxuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-config-service/10-CONTEXT.md
@.planning/phases/10-config-service/10-RESEARCH.md
@.planning/phases/10-config-service/10-PATTERNS.md
@.planning/phases/10-config-service/10-01-PLAN.md

<interfaces>
<!-- Key existing functions to reuse -->

From src/lib/security/token-check.ts:
```typescript
export function maskToken(token: string): string {
  if (!token || token.length < 4) {
    return '****';
  }
  return `...${token.slice(-4)}`;
}
```

From src/lib/services/types.ts:
```typescript
export class ServiceError extends Error {
  public readonly code: string;
  public readonly context?: Record<string, unknown>;
  constructor(message: string, code: string, context?: Record<string, unknown>);
}
```

From src/lib/types/api-config.ts (created in Plan 01):
```typescript
export type ApiConfig = z.infer<typeof ApiConfigSchema>;
export type MaskedApiConfig = Omit<ApiConfig, 'apiKey'> & { apiKey: string };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create API key security utilities</name>
  <files>src/lib/security/api-key.ts, src/lib/security/api-key.test.ts</files>
  <read_first>
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/security/token-check.ts (maskToken to reuse)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/security/token-check.test.ts (test pattern)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/services/types.ts (ServiceError)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/types/api-config.ts (ApiConfig, MaskedApiConfig)
  </read_first>
  <behavior>
    - Test 1: maskApiKey returns '...last4' format for keys >= 4 chars
    - Test 2: maskApiKey returns '****' for keys < 4 chars
    - Test 3: maskApiKey returns '****' for empty string
    - Test 4: applyMaskedApiKey returns MaskedApiConfig with masked apiKey
    - Test 5: validateNoCliApiKey throws SecurityError for '--api-key' arg
    - Test 6: validateNoCliApiKey throws SecurityError for '--apiKey' arg
    - Test 7: validateNoCliApiKey throws SecurityError for '-k' arg
    - Test 8: validateNoCliApiKey throws SecurityError for 'apiKey=' substring
    - Test 9: validateNoCliApiKey does not throw for safe args
  </behavior>
  <action>
    Per D-09/D-10: Create src/lib/security/api-key.ts:

    1. Imports:
       - maskToken from './token-check.js' (D-10: reuse existing)
       - ServiceError from '../services/types.js'
       - type ApiConfig, type MaskedApiConfig from '../types/api-config.js'

    2. Export function maskApiKey(apiKey: string): string
       - Return maskToken(apiKey) directly (D-10 reuse)
       - Shows last 4 chars for CFG-04

    3. Export function applyMaskedApiKey(config: ApiConfig): MaskedApiConfig
       - Return { ...config, apiKey: maskApiKey(config.apiKey) }
       - Used for display contexts: preview, diff, logs

    4. Export function validateNoCliApiKey(args: string[]): void
       - Patterns to check: '--api-key', '--apiKey', '-k', 'apiKey='
       - Loop through args, check each pattern
       - If any match: throw new ServiceError(
         'API key cannot be passed via command-line arguments. Use config file or stdin.',
         'SECURITY_VIOLATION'
       )
       - Per D-09 and SEC-01

    Create test file src/lib/security/api-key.test.ts:
    - Import { describe, it, expect } from 'vitest'
    - Import maskApiKey, applyMaskedApiKey, validateNoCliApiKey from './api-key.js'
    - Import ServiceError from '../services/types.js'
    - Test maskApiKey with various lengths
    - Test applyMaskedApiKey preserves other fields
    - Test validateNoCliApiKey throws for each pattern
    - Test validateNoCliApiKey passes for safe args like ['--config', 'my-config']
  </action>
  <verify>
    <automated>vitest run src/lib/security/api-key.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/security/api-key.ts contains "export function maskApiKey"
    - src/lib/security/api-key.ts contains "import { maskToken } from './token-check.js'"
    - src/lib/security/api-key.ts contains "return maskToken(apiKey)"
    - src/lib/security/api-key.ts contains "export function applyMaskedApiKey"
    - src/lib/security/api-key.ts contains "export function validateNoCliApiKey"
    - src/lib/security/api-key.ts contains "'SECURITY_VIOLATION'"
    - src/lib/security/api-key.ts contains "'--api-key'" and "'--apiKey'" and "'-k'"
    - vitest run src/lib/security/api-key.test.ts exits 0
  </acceptance_criteria>
  <done>API key masking and CLI args enforcement implemented (CFG-04, SEC-01)</done>
</task>

<task type="auto">
  <name>Task 2: Create security module barrel export</name>
  <files>src/lib/security/index.ts</files>
  <read_first>
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/types/index.ts (barrel pattern)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/security/api-key.ts (exports to add)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/security/token-check.ts (existing exports)
  </read_first>
  <action>
    Create src/lib/security/index.ts (NEW file - security module didn't have barrel export):

    1. Add header comment:
       /**
        * Security Module - Barrel Export
        *
        * Central export point for all security utilities.
        */

    2. Export existing token-check:
       export * from './token-check.js';

    3. Export new api-key:
       export * from './api-key.js';

    This enables: import { maskApiKey, validateNoCliApiKey } from '../security/index.js'
  </action>
  <verify>
    <automated>vitest run --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/security/index.ts contains "export * from './token-check.js'"
    - src/lib/security/index.ts contains "export * from './api-key.js'"
    - File exists at /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/security/index.ts
  </acceptance_criteria>
  <done>Security module barrel export created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Service | validateNoCliApiKey blocks apiKey in args at service boundary |
| Service → Display | maskApiKey applied before any display/log output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | Shell history | mitigate | D-09: validateNoCliApiKey blocks '--api-key' args, prevents HISTIGNORE leakage |
| T-10-02 | Information Disclosure | Process listing | mitigate | D-09: validateNoCliApiKey blocks CLI args, prevents 'ps aux' visibility |
| T-10-03 | Information Disclosure | Logs/crash dumps | mitigate | D-10: maskApiKey shows only last 4 chars, applyMaskedApiKey for display contexts |
</threat_model>

<verification>
- maskApiKey returns ...last4 format
- validateNoCliApiKey throws SecurityError for apiKey patterns
- Barrel export enables unified import
</verification>

<success_criteria>
- API key masked in display contexts (CFG-04)
- CLI args containing apiKey rejected (SEC-01)
- maskToken reused via maskApiKey (D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/10-config-service/10-03-SUMMARY.md`
</output>
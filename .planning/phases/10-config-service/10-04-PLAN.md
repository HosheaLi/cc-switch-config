---
phase: 10
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - src/lib/types/index.ts
  - src/lib/store/index.ts
  - src/lib/services/index.ts
  - src/lib/services/config-service.ts
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-04
  - SEC-01
  - SEC-03
user_setup: []
must_haves:
  truths:
    - "ApiConfig types exported from types/index.ts"
    - "ApiConfigStore exported from store/index.ts"
    - "ApiService exported from services/index.ts"
    - "ConfigService.applyApiConfig method available"
    - "All modules integrated via barrel exports"
  artifacts:
    - path: "src/lib/types/index.ts"
      provides: "Barrel export including api-config and replacement"
      contains: "export * from './api-config.js'"
    - path: "src/lib/store/index.ts"
      provides: "Barrel export including ApiConfigStore"
      contains: "export { ApiConfigStore } from './api-config.js'"
    - path: "src/lib/services/index.ts"
      provides: "Barrel export including ApiService"
      contains: "export { ApiService } from './api-service.js'"
    - path: "src/lib/services/config-service.ts"
      provides: "applyApiConfig method using replaceEnvModel"
      contains: "async applyApiConfig"
  key_links:
    - from: "src/lib/services/index.ts"
      to: "src/lib/services/api-service.ts"
      via: "barrel export"
      pattern: "export { ApiService }"
    - from: "src/lib/services/config-service.ts"
      to: "src/lib/types/replacement.ts"
      via: "replaceEnvModel import"
      pattern: "import { replaceEnvModel } from '../types/replacement.js'"
---

<objective>
Integrate all Phase 10 modules via barrel exports and add applyApiConfig to ConfigService.

Purpose: Complete integration of ApiConfig system into existing module structure.
Output: Barrel exports updated, ConfigService.applyApiConfig method added.
</objective>

<execution_context>
@/Users/lihaoxuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lihaoxuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-config-service/10-CONTEXT.md
@.planning/phases/10-config-service/10-RESEARCH.md
@.planning/phases/10-config-service/10-PATTERNS.md
@.planning/phases/10-config-service/10-01-SUMMARY.md
@.planning/phases/10-config-service/10-02-SUMMARY.md
@.planning/phases/10-config-service/10-03-SUMMARY.md

<interfaces>
<!-- Files created in prior waves - must be exported -->

From src/lib/types/api-config.ts (Wave 1):
```typescript
export const ApiConfigSchema, ApiConfigModeSchema;
export type ApiConfig, MaskedApiConfig;
```

From src/lib/types/replacement.ts (Wave 1):
```typescript
export function replaceEnvModel(existing: ClaudeSettings, apiConfig: ApiConfig): ClaudeSettings;
```

From src/lib/store/api-config.ts (Wave 2):
```typescript
export class ApiConfigStore;
export interface ApiConfigStoreData;
```

From src/lib/services/api-service.ts (Wave 2):
```typescript
export class ApiService;
```

From src/lib/security/api-key.ts (Wave 2):
```typescript
export function maskApiKey, applyMaskedApiKey, validateNoCliApiKey;
```

From src/lib/security/index.ts (Wave 2):
```typescript
export * from './token-check.js';
export * from './api-key.js';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update types module barrel export</name>
  <files>src/lib/types/index.ts</files>
  <read_first>
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/types/index.ts (current state)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/types/api-config.ts (created in Plan 01)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/types/replacement.ts (created in Plan 01)
  </read_first>
  <action>
    Modify src/lib/types/index.ts to add exports:

    Add after existing exports (preserving all existing exports):
    - export * from './api-config.js';
    - export * from './replacement.js';

    Order: add at end of file after export-schema.js export.

    Final file should have:
    - export * from './config.js';
    - export * from './validation.js';
    - export * from './merge.js';
    - export * from './provider.js';
    - export * from './export-schema.js';
    - export * from './api-config.js';  // NEW
    - export * from './replacement.js'; // NEW
  </action>
  <verify>
    <automated>vitest run --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/types/index.ts contains "export * from './api-config.js'"
    - src/lib/types/index.ts contains "export * from './replacement.js'"
    - Existing exports preserved (config, validation, merge, provider, export-schema)
  </acceptance_criteria>
  <done>Types module barrel export includes api-config and replacement</done>
</task>

<task type="auto">
  <name>Task 2: Update store module barrel export</name>
  <files>src/lib/store/index.ts</files>
  <read_first>
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/store/index.ts (current state)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/store/api-config.ts (created in Plan 02)
  </read_first>
  <action>
    Modify src/lib/store/index.ts to add exports:

    Add after TemplateStore exports:
    - export { ApiConfigStore } from './api-config.js';
    - export type { ApiConfigStoreData } from './api-config.js';

    Final file should have (preserving all existing):
    - export { readConfig, writeConfig, configExists } from './config.js';
    - export { TemplateStore } from './template.js';
    - export type { TemplateStoreData } from './template.js';
    - export { ApiConfigStore } from './api-config.js';  // NEW
    - export type { ApiConfigStoreData } from './api-config.js';  // NEW
    - export { ProjectIndex } from './project.js';
    - ... (rest of existing exports)
  </action>
  <verify>
    <automated>vitest run --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/store/index.ts contains "export { ApiConfigStore } from './api-config.js'"
    - src/lib/store/index.ts contains "export type { ApiConfigStoreData } from './api-config.js'"
    - Existing exports preserved
  </acceptance_criteria>
  <done>Store module barrel export includes ApiConfigStore</done>
</task>

<task type="auto">
  <name>Task 3: Update services module barrel export</name>
  <files>src/lib/services/index.ts</files>
  <read_first>
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/services/index.ts (current state)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/services/api-service.ts (created in Plan 02)
  </read_first>
  <action>
    Modify src/lib/services/index.ts to add exports:

    Add after TemplateService export:
    - export { ApiService } from './api-service.js';

    Also add ApiConfig type re-export for convenience:
    - export type { ApiConfig, MaskedApiConfig } from '../types/api-config.js';

    Final file should have (preserving all existing):
    - export { ConfigService } from './config-service.js';
    - export { ProjectService } from './project-service.js';
    - export { TemplateService } from './template-service.js';
    - export { ApiService } from './api-service.js';  // NEW
    - ... (rest of existing exports)
    - export type { ApiConfig, MaskedApiConfig } from '../types/api-config.js';  // NEW convenience
  </action>
  <verify>
    <automated>vitest run --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/services/index.ts contains "export { ApiService } from './api-service.js'"
    - src/lib/services/index.ts contains "export type { ApiConfig } from '../types/api-config.js'"
    - Existing exports preserved
  </acceptance_criteria>
  <done>Services module barrel export includes ApiService and ApiConfig types</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add applyApiConfig method to ConfigService</name>
  <files>src/lib/services/config-service.ts</files>
  <read_first>
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/services/config-service.ts (current state)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/types/replacement.ts (replaceEnvModel)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/types/api-config.ts (ApiConfig type)
    - /Users/lihaoxuan/code/P07_CCAPISwitch/src/lib/security/api-key.ts (maskApiKey)
  </read_first>
  <behavior>
    - Test 1: applyApiConfig replaces env/model fields, preserves permissions/hooks/mcpServers
    - Test 2: applyApiConfig throws CONFIG_NOT_FOUND for missing config name
    - Test 3: applyApiConfig masks apiKey in error context (never exposed)
  </behavior>
  <action>
    Modify src/lib/services/config-service.ts to add applyApiConfig method:

    1. Add imports at top:
       - import type { ApiConfig } from '../types/api-config.js';
       - import { replaceEnvModel } from '../types/replacement.js';
       - import { maskApiKey } from '../security/api-key.js';

    2. Add method after applyTemplate method:
       ```typescript
       /**
        * Apply ApiConfig to project configuration.
        *
        * Per CFG-02: Precise field replacement - only env/model changed.
        * Preserves permissions, hooks, mcpServers.
        *
        * @param projectPath - Root path of the project
        * @param apiConfig - ApiConfig to apply
        * @throws ServiceError on write failure
        */
       async applyApiConfig(projectPath: string, apiConfig: ApiConfig): Promise<void> {
         const existing = await this.readProjectConfig(projectPath) ?? {};

         // Per CFG-02: Precise replacement using replaceEnvModel
         const merged = replaceEnvModel(existing, apiConfig);

         await this.writeProjectConfig(projectPath, merged);
       }
       ```

    3. Note: This method takes ApiConfig directly (not config name).
       Phase 11 CLI commands will use ApiService to get config by name,
       then call ConfigService.applyApiConfig with the config object.

    Run existing tests to ensure no regression:
    - vitest run src/lib/services/config-service.test.ts
  </action>
  <verify>
    <automated>vitest run src/lib/services/config-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/services/config-service.ts contains "import type { ApiConfig } from '../types/api-config.js'"
    - src/lib/services/config-service.ts contains "import { replaceEnvModel } from '../types/replacement.js'"
    - src/lib/services/config-service.ts contains "async applyApiConfig(projectPath: string, apiConfig: ApiConfig)"
    - src/lib/services/config-service.ts contains "replaceEnvModel(existing, apiConfig)"
    - vitest run src/lib/services/config-service.test.ts exits 0
  </acceptance_criteria>
  <done>ConfigService.applyApiConfig method added using replaceEnvModel</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Services → Types | replaceEnvModel imported, precise replacement enforced |
| ConfigService → Security | maskApiKey imported for error context masking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Information Disclosure | ServiceError context | mitigate | Never include raw apiKey in error context, use maskApiKey if needed |
</threat_model>

<verification>
- All barrel exports updated
- ConfigService.applyApiConfig method works
- Existing tests pass
</verification>

<success_criteria>
- ApiConfigStore accessible via store/index.ts
- ApiService accessible via services/index.ts
- ConfigService.applyApiConfig uses replaceEnvModel
- All existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/10-config-service/10-04-SUMMARY.md`
</output>
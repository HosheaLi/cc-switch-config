---
phase: 13
slug: switch-flow
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/cli/commands/switch.test.ts
  - src/cli/utils/diff-render.test.ts
  - src/cli/prompts/components/select-api-config.test.ts
autonomous: true
requirements:
  - CFG-05
  - ONB-06
user_setup: []
must_haves:
  truths:
    - Test scaffolds exist for switch command, diff-render, and selectApiConfig
    - All test files export describe/it/expect structure
    - Tests are ready for TDD implementation
  artifacts:
    - path: src/cli/commands/switch.test.ts
      provides: Test scaffold for switch command
      exports: ['describe', 'it', 'expect']
    - path: src/cli/utils/diff-render.test.ts
      provides: Test scaffold for diff rendering utility
      exports: ['describe', 'it', 'expect']
    - path: src/cli/prompts/components/select-api-config.test.ts
      provides: Test scaffold for selectApiConfig component
      exports: ['describe', 'it', 'expect']
  key_links:
    - from: src/cli/commands/switch.test.ts
      to: src/cli/commands/switch.ts
      via: import switch command module
      pattern: import.*switch
---

<objective>
Create Wave 0 test scaffolds for Phase 13 implementation.

Purpose: Tests must exist before implementation per Nyquist rule. TDD requires RED state before GREEN state.
Output: Three test scaffold files ready for test writing.
</objective>

<execution_context>
@/Users/lihaoxuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lihaoxuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-switch-flow/13-CONTEXT.md
@.planning/phases/13-switch-flow/13-RESEARCH.md
@.planning/phases/13-switch-flow/13-VALIDATION.md

# Existing test patterns reference
@src/cli/utils/diff.test.ts
@src/cli/prompts/components/confirm-action.ts
</context>

<interfaces>
<!-- Key interfaces from existing codebase that tests will import -->

From src/cli/utils/diff.ts:
```typescript
export interface DiffLine {
  type: 'added' | 'removed' | 'modified';
  path: string;
  value?: unknown;
  before?: unknown;
  after?: unknown;
}
export function generateUnifiedDiff(before: ClaudeSettings, after: ClaudeSettings): DiffLine[];
```

From src/lib/types/api-config.ts:
```typescript
export interface ApiConfig {
  name: string;
  apiKey: string;
  baseUrl: string;
  modelName: string;
  mode: 'unified' | 'granular';
  env?: Record<string, string>;
  createdAt?: string;
  updatedAt?: string;
}
```

From src/lib/store/project.ts:
```typescript
export interface ProjectEntry {
  id: string;
  name: string;
  path: string;
  activeConfig: string | null;
  lastModified: string;
}
```

From src/cli/prompts/utils/handle-cancel.ts:
```typescript
export interface PromptResult<T> {
  value: T | null;
  cancelled: boolean;
}
``</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create switch.test.ts scaffold</name>
  <files>src/cli/commands/switch.test.ts</files>
  <behavior>
    - Test scaffold exists with describe('switch command') block
    - Imports switch module (will fail until implemented - that's expected for Wave 0)
    - Placeholder test cases for: argument parsing, project lookup, config selection, diff preview, confirmation, cancellation
  </behavior>
  <read_first>
    - src/cli/commands/switch.ts (existing old switch command - will be refactored)
    - src/cli/utils/diff.test.ts (test pattern reference)
    - src/lib/store/api-config.ts (ApiConfigStore interface)
    - src/lib/store/project.ts (ProjectIndex interface)
  </read_first>
  <action>Create test scaffold file with:
1. Import statements for: vitest (describe/it/expect), chalk, ApiConfigStore, ProjectIndex, ConfigService, generateUnifiedDiff, renderDiff (to be created), selectApiConfig (to be created), confirmAction, maskApiKey, replaceEnvModel
2. Mock setup for stores and services using vi.mock()
3. Describe block 'switch command' with nested describe blocks for:
   - 'argument parsing' (D-01, D-02: project required, config optional)
   - 'project lookup' (D-02: error if project not found)
   - 'config selection' (D-03: selectApiConfig if config omitted)
   - 'diff preview' (D-04, D-05, D-06: unified diff with ANSI colors)
   - 'confirmation' (D-07, D-08: confirmAction with defaultChoice=false)
   - 'cancellation' (D-09: Ctrl+C shows cancel message)
4. Placeholder it() blocks with todo() markers - tests will be filled in Wave 1-2
5. No actual test implementations yet - just scaffolding for TDD RED state</action>
  <verify>
    <automated>test -f src/cli/commands/switch.test.ts && grep -q "describe('switch command'" src/cli/commands/switch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/cli/commands/switch.test.ts exists
    - Contains "describe('switch command'" 
    - Contains import statements for ApiConfigStore, ProjectIndex, ConfigService
    - Contains nested describe blocks for argument parsing, project lookup, config selection, diff preview, confirmation, cancellation
  </acceptance_criteria>
  <done>Test scaffold file exists with structure matching TDD pattern from diff.test.ts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create diff-render.test.ts scaffold</name>
  <files>src/cli/utils/diff-render.test.ts</files>
  <behavior>
    - Test scaffold exists with describe('renderDiff') block
    - Placeholder test cases for: header format, removed lines (red), added lines (green), modified lines (yellow), empty diff, value truncation
  </behavior>
  <read_first>
    - src/cli/utils/diff.ts (DiffLine interface reference)
    - src/cli/utils/diff.test.ts (test pattern reference)
    - src/cli/prompts/components/confirm-action.ts (chalk usage pattern)
  </read_first>
  <action>Create test scaffold file with:
1. Import statements for: vitest, chalk, DiffLine type from diff.ts
2. Describe block 'renderDiff' with nested describe blocks for:
   - 'header format' (D-05: --- a/ and +++ b/ format)
   - 'removed lines' (D-06: chalk.red for deleted fields)
   - 'added lines' (D-06: chalk.green for added fields)
   - 'modified lines' (D-06: yellow for modified fields with before/after)
   - 'empty diff' (shows "无变化" message)
   - 'value truncation' (long strings truncated to 50 chars)
3. Placeholder it() blocks with todo() markers
4. Mock chalk.red/green/yellow/gray using vi.fn() for output capture
5. No actual implementations - just scaffolding</action>
  <verify>
    <automated>test -f src/cli/utils/diff-render.test.ts && grep -q "describe('renderDiff'" src/cli/utils/diff-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/cli/utils/diff-render.test.ts exists
    - Contains "describe('renderDiff'"
    - Contains import for DiffLine type
    - Contains nested describe blocks for header format, removed/added/modified lines, empty diff, value truncation
  </acceptance_criteria>
  <done>Test scaffold file exists with structure for ANSI color output testing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create select-api-config.test.ts scaffold</name>
  <files>src/cli/prompts/components/select-api-config.test.ts</files>
  <behavior>
    - Test scaffold exists with describe('selectApiConfig') block
    - Placeholder test cases for: empty configs warning, single config selection, multiple configs autocomplete, cancellation handling, masked API key in description
  </behavior>
  <read_first>
    - src/cli/prompts/components/select-template.ts (pattern reference for selection component)
    - src/cli/prompts/utils/handle-cancel.ts (PromptResult interface)
    - src/lib/store/api-config.ts (ApiConfig interface)
    - src/lib/security/api-key.ts (maskApiKey function)
  </read_first>
  <action>Create test scaffold file with:
1. Import statements for: vitest, prompts, ApiConfig type, maskApiKey, selectApiConfig (to be created)
2. Describe block 'selectApiConfig' with nested describe blocks for:
   - 'empty configs' (shows warning message + hint to create config)
   - 'single config' (shows selection UI even for single - per D-03)
   - 'multiple configs' (uses autocomplete for >5 configs per TUI-04)
   - 'cancellation' (Ctrl+C returns null per TUI-05)
   - 'description format' (shows modelName @ baseUrl, no API key exposure)
3. Mock prompts using vi.mock('prompts')
4. Mock promptWithCancel using vi.mock('../utils/handle-cancel')
5. Placeholder it() blocks with todo() markers</action>
  <verify>
    <automated>test -f src/cli/prompts/components/select-api-config.test.ts && grep -q "describe('selectApiConfig'" src/cli/prompts/components/select-api-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/cli/prompts/components/select-api-config.test.ts exists
    - Contains "describe('selectApiConfig'"
    - Contains import for ApiConfig type
    - Contains nested describe blocks for empty configs, single/multiple selection, cancellation, description format
  </acceptance_criteria>
  <done>Test scaffold file exists with structure for prompts selection component testing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → switch command | Untrusted user input crosses here (project/config names) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | diff output | mitigate | maskApiKey applied before diff rendering (CFG-04) |
| T-13-02 | Tampering | confirmAction default | mitigate | defaultChoice=false prevents accidental application (D-08) |

Note: Wave 0 test scaffolds have no executable code - threats apply to implementation phase.
</threat_model>

<verification>
After completing all tasks:
1. Run `npm test -- --run src/cli/commands/switch.test.ts src/cli/utils/diff-render.test.ts src/cli/prompts/components/select-api-config.test.ts` - should pass (todo() tests pass by default)
2. All three test scaffold files exist with proper structure
</verification>

<success_criteria>
- [ ] src/cli/commands/switch.test.ts exists with describe('switch command')
- [ ] src/cli/utils/diff-render.test.ts exists with describe('renderDiff')
- [ ] src/cli/prompts/components/select-api-config.test.ts exists with describe('selectApiConfig')
- [ ] All test scaffolds follow vitest pattern (describe/it/expect)
- [ ] All placeholder tests use todo() markers for TDD RED state
</success_criteria>

<output>
After completion, create `.planning/phases/13-switch-flow/13-01-SUMMARY.md`
</output>
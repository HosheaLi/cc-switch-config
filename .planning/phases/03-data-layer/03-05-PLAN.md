---
phase: 03-data-layer
plan: 05
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified: [src/lib/store/state.ts, src/lib/store/state.test.ts, src/lib/store/index.ts]
autonomous: true
requirements: [DATA-05]
user_setup: []

must_haves:
  truths:
    - "AppState can store and retrieve active project ID"
    - "AppState can store and retrieve UI preferences"
    - "AppState maintains recent projects list (max 10)"
    - "State persists across restarts"
    - "All store modules exported from single barrel file"
  artifacts:
    - path: "src/lib/store/state.ts"
      provides: "AppState class using conf package"
      min_lines: 60
      exports: ["AppState", "AppStateData"]
    - path: "src/lib/store/state.test.ts"
      provides: "Unit tests for AppState"
      min_lines: 50
    - path: "src/lib/store/index.ts"
      provides: "Barrel export for all store modules"
      exports: ["ConfigRepository", "TemplateStore", "ProjectIndex", "FileWatcher", "AppState"]
  key_links:
    - from: "src/lib/store/state.ts"
      to: "conf"
      via: "npm package"
      pattern: "import.*Conf.*from.*conf"
    - from: "src/lib/store/index.ts"
      to: "src/lib/store/*.ts"
      via: "barrel imports"
      pattern: "export.*from.*config.js|template.js|project.js|watcher.js|state.js"
---

<objective>
创建 AppState 类，使用 conf 包管理应用持久化状态（当前激活项目、UI 偏好、最近项目列表）。创建 barrel export 文件统一导出所有 store 模块。

Purpose: 提供全局状态管理，支持 TUI 记录用户偏好和会话状态。Barrel export 简化导入路径。
Output: src/lib/store/state.ts (AppState 类) + 测试文件 + src/lib/store/index.ts barrel export。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-layer/03-CONTEXT.md
@.planning/phases/03-data-layer/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase and research. -->

From conf 15.1 API (already installed):
```typescript
import Conf from 'conf';

interface ConfOptions<T> {
  projectName: string; // XDG directory name
  defaults?: T; // Default values
}

class Conf<T> {
  get<K extends keyof T>(key: K): T[K];
  set<K extends keyof T>(key: K, value: T[K]): void;
  path: string; // File path for direct access
  clear(): void;
}
```

From src/lib/store/config.ts (to be created in Plan 01):
```typescript
export async function readConfig(filepath: string): Promise<ClaudeSettings | null>;
export async function writeConfig(filepath: string, config: ClaudeSettings): Promise<void>;
export async function configExists(filepath: string): Promise<boolean>;
```

From src/lib/store/template.ts (to be created in Plan 02):
```typescript
export class TemplateStore { ... }
export interface TemplateStoreData { ... }
```

From src/lib/store/project.ts (to be created in Plan 03):
```typescript
export class ProjectIndex { ... }
export interface ProjectEntry { ... }
export interface ProjectIndexData { ... }
```

From src/lib/store/watcher.ts (to be created in Plan 04):
```typescript
export class FileWatcher { ... }
export interface WatcherOptions { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AppState class</name>
  <files>src/lib/store/state.ts, src/lib/store/state.test.ts</files>
  <read_first>
    - package.json (确认 conf 依赖已安装)
    - src/lib/types/provider.ts (了解 TemplateConfig 用于 recentTemplates)
  </read_first>
  <behavior>
    - Test 1: get returns default value for unset key
    - Test 2: set stores value and get retrieves it
    - Test 3: getActiveProject returns current active project ID
    - Test 4: setActiveProject updates active project and recent list
    - Test 5: recent projects list capped at 10 entries
    - Test 6: setActiveProject moves existing project to front of recent list
    - Test 7: clear resets all state to defaults
    - Test 8: state persists across AppState instances
  </behavior>
  <action>
    在 src/lib/store/state.ts 实现 AppState 类：

    1. AppStateData 类型：
       ```typescript
       export interface AppStateData {
         activeProjectId: string | null;
         lastUsedTemplate: string | null;
         uiPreferences: {
           theme: 'dark' | 'light';
           showPreview: boolean;
         };
         recentProjects: string[]; // Max 10 recent project IDs
       }
       ```

    2. AppState 类：
       - 私有属性：conf (Conf<AppStateData> 实例)

       构造函数：
       ```typescript
       constructor() {
         this.conf = new Conf<AppStateData>({
           projectName: 'cc-config-switch',
           defaults: {
             activeProjectId: null,
             lastUsedTemplate: null,
             uiPreferences: {
               theme: 'dark',
               showPreview: true,
             },
             recentProjects: [],
           },
         });
       }
       ```

       公开方法：
       - get<K extends keyof AppStateData>(key: K): AppStateData[K]
       - set<K extends keyof AppStateData>(key: K, value: AppStateData[K]): void
       - getActiveProject(): string | null
       - setActiveProject(projectId: string): void — 同时更新 recentProjects 列表
       - getFilePath(): string — 返回 conf.path
       - clear(): void

    3. 导出：
       ```typescript
       export class AppState { ... }
       export interface AppStateData { ... }
       ```

    导入：
    - import Conf from 'conf';

    在 src/lib/store/state.test.ts 编写测试：
    - 使用 vitest 的 describe/it/expect
    - 使用 beforeAll/afterAll 清理测试状态
    - 测试时使用单独的 projectName 避免污染真实状态
  </action>
  <verify>
    <automated>vitest run src/lib/store/state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export class AppState" src/lib/store/state.ts >= 1
    - grep -c "export interface AppStateData" src/lib/store/state.ts >= 1
    - grep "import.*Conf.*from.*conf" src/lib/store/state.ts
    - grep "projectName.*cc-config-switch" src/lib/store/state.ts
    - grep -c "getActiveProject" src/lib/store/state.ts >= 1
    - grep -c "setActiveProject" src/lib/store/state.ts >= 1
    - grep "recentProjects" src/lib/store/state.ts
    - vitest run src/lib/store/state.test.ts 显示所有测试通过
  </acceptance_criteria>
  <done>
    AppState 类完成，支持 get/set/getActiveProject/setActiveProject/clear 操作，测试覆盖率 >=80%。
  </done>
</task>

<task type="auto">
  <name>Task 2: Create barrel export file</name>
  <files>src/lib/store/index.ts</files>
  <read_first>
    - src/lib/store/config.ts (确认导出函数名)
    - src/lib/store/template.ts (确认导出类名)
    - src/lib/store/project.ts (确认导出类名)
    - src/lib/store/watcher.ts (确认导出类名)
    - src/lib/store/state.ts (确认导出类名)
    - src/lib/types/index.ts (参考 barrel export 模式)
  </read_first>
  <action>
    创建 src/lib/store/index.ts 作为 barrel export：

    ```typescript
    /**
     * Store Module - Barrel Export
     *
     * Central export point for all data layer modules.
     * Per D-08: src/lib/store/ directory unified management.
     *
     * Import examples:
     * - import { readConfig, writeConfig } from './lib/store/index.js';
     * - import { TemplateStore } from './lib/store/index.js';
     * - import { ProjectIndex } from './lib/store/index.js';
     * - import { AppState } from './lib/store/index.js';
     */

    // Config Repository (functions)
    export { readConfig, writeConfig, configExists } from './config.js';

    // Template Store (class)
    export { TemplateStore } from './template.js';
    export type { TemplateStoreData, TemplateConfig } from './template.js';

    // Project Index (class)
    export { ProjectIndex } from './project.js';
    export type { ProjectEntry, ProjectIndexData } from './project.js';

    // File Watcher (class)
    export { FileWatcher } from './watcher.js';
    export type { WatcherOptions, WatcherCallback } from './watcher.js';

    // App State (class)
    export { AppState } from './state.js';
    export type { AppStateData } from './state.js';
    ```

    注意：
    - 使用 .js 后缀（ESM NodeNext）
    - 类型导出使用 `export type { ... }`
    - 注释说明导入示例
  </action>
  <verify>
    <automated>grep -c "export" src/lib/store/index.ts && node -e "import('./src/lib/store/index.js').then(m => console.log(Object.keys(m)))"
---
phase: 03-data-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/store/template.ts, src/lib/store/template.test.ts]
autonomous: true
requirements: [DATA-02]
user_setup: []

must_haves:
  truths:
    - "TemplateStore can read templates from templates.json"
    - "TemplateStore can create new templates"
    - "TemplateStore can update existing templates"
    - "TemplateStore can delete templates"
    - "Templates persist across restarts"
    - "Template operations validate schema before persisting"
  artifacts:
    - path: "src/lib/store/template.ts"
      provides: "TemplateStore class for template CRUD"
      min_lines: 100
      exports: ["TemplateStore", "TemplateStoreData"]
    - path: "src/lib/store/template.test.ts"
      provides: "Unit tests for TemplateStore"
      min_lines: 120
    - path: "getConfigDir()/templates.json"
      provides: "Template storage file"
      schema: "TemplateStoreSchema"
  key_links:
    - from: "src/lib/store/template.ts"
      to: "src/lib/types/provider.ts"
      via: "TemplateConfigSchema"
      pattern: "import.*TemplateConfigSchema.*from.*provider.js"
    - from: "src/lib/store/template.ts"
      to: "src/lib/paths/xdg.ts"
      via: "getConfigDir"
      pattern: "import.*getConfigDir.*from.*xdg.js"
    - from: "src/lib/store/template.ts"
      to: "src/lib/file-system/json.ts"
      via: "readJSON/writeJSON"
      pattern: "import.*from.*json.js"
    - from: "src/lib/store/template.ts"
      to: "src/lib/file-system/backup.ts"
      via: "createBackup"
      pattern: "import.*createBackup.*from.*backup.js"
---

<objective>
创建 TemplateStore 类，管理用户自定义的 API provider 模板。模板存储在 XDG config dir 的 templates.json 文件中。

Purpose: 提供模板 CRUD 操作，支持用户创建、更新、删除、查询自定义 API provider 模板。
Output: src/lib/store/template.ts (TemplateStore 类) + 测试文件，模板存储于 getConfigDir()/templates.json。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-layer/03-CONTEXT.md
@.planning/phases/03-data-layer/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/types/provider.ts:
```typescript
export const TemplateConfigSchema = z.object({
  name: z.string().min(1, 'Template name required'),
  description: z.string().optional(),
  provider: ApiProviderConfigSchema,
  tags: z.array(z.string()).optional(),
  createdAt: z.string().datetime().optional(),
  updatedAt: z.string().datetime().optional(),
}).strict();
export type TemplateConfig = z.infer<typeof TemplateConfigSchema>;

export const TemplateStoreSchema = z.object({
  version: z.number().int().optional(),
  templates: z.record(z.string(), TemplateConfigSchema),
}).strict();
export type TemplateStore = z.infer<typeof TemplateStoreSchema>;

export const ApiProviderConfigSchema = z.object({
  name: z.string().min(1, 'Provider name required'),
  baseUrl: z.string().url('Valid URL required'),
  authType: AuthTypeSchema,
  headers: z.record(z.string(), z.string()).optional(),
  env: z.record(z.string(), z.string()).optional(),
}).strict();
export type ApiProviderConfig = z.infer<typeof ApiProviderConfigSchema>;

export const AuthTypeSchema = z.enum(['token', 'header', 'custom']);
export type AuthType = z.infer<typeof AuthTypeSchema>;
```

From src/lib/paths/xdg.ts:
```typescript
export function getConfigDir(): string;
export function getDataDir(): string;
```

From src/lib/file-system/json.ts:
```typescript
export async function readJSON<T = unknown>(filepath: string): Promise<T | null>;
export async function writeJSON(filepath: string, data: unknown): Promise<void>;
```

From src/lib/file-system/backup.ts:
```typescript
export async function createBackup(filepath: string): Promise<string | null>;
```

From src/lib/types/validation.ts:
```typescript
export class ValidationError extends Error {
  public readonly issues: z.core.$ZodIssue[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TemplateStore class</name>
  <files>src/lib/store/template.ts, src/lib/store/template.test.ts</files>
  <read_first>
    - src/lib/types/provider.ts (了解 TemplateConfigSchema, TemplateStoreSchema, ApiProviderConfigSchema)
    - src/lib/paths/xdg.ts (了解 getConfigDir 函数)
    - src/lib/file-system/json.ts (了解 readJSON/writeJSON)
    - src/lib/file-system/backup.ts (了解 createBackup)
    - src/lib/types/validation.ts (了解 ValidationError 类)
  </read_first>
  <behavior>
    - Test 1: getAll returns empty object for new store
    - Test 2: set creates new template with valid data
    - Test 3: set throws ValidationError for invalid template
    - Test 4: get returns template by name
    - Test 5: get returns null for non-existent template
    - Test 6: delete removes existing template and returns true
    - Test 7: delete returns false for non-existent template
    - Test 8: list returns all template names as array
    - Test 9: templates persist after store reload
  </behavior>
  <action>
    在 src/lib/store/template.ts 实现 TemplateStore 类：

    1. TemplateStoreData 类型（与 TemplateStoreSchema 对应）：
       ```typescript
       export interface TemplateStoreData {
         version?: number;
         templates: Record<string, TemplateConfig>;
       }
       ```

    2. TemplateStore 类：
       - 私有属性：filePath (getConfigDir()/templates.json), data (缓存)
       - 私有方法 load(): Promise<TemplateStoreData> — 懒加载，首次访问时读取文件，不存在时返回默认值 { version: 1, templates: {} }
       - 私有方法 save(): Promise<void> — 写入文件，更新缓存

       公开方法：
       - async getAll(): Promise<Record<string, TemplateConfig>> — 返回所有模板
       - async get(name: string): Promise<TemplateConfig | null> — 按名称获取
       - async set(name: string, template: TemplateConfig): Promise<void> — 创建/更新模板
         - 验证模板（TemplateConfigSchema.safeParse）
         - 失败时抛出 ValidationError
         - 添加 createdAt/updatedAt 时间戳
         - 创建备份（如果文件存在）
         - 写入文件
       - async delete(name: string): Promise<boolean> — 删除模板，返回是否成功
         - 模板不存在返回 false
         - 创建备份后删除
       - async list(): Promise<string[]> — 返回所有模板名称数组

    3. 导出：
       ```typescript
       export class TemplateStore { ... }
       export interface TemplateStoreData { ... }
       ```

    导入路径使用 .js 后缀：
    - import { readJSON, writeJSON, exists } from '../file-system/json.js';
    - import { createBackup } from '../file-system/backup.js';
    - import { getConfigDir } from '../paths/xdg.js';
    - import { TemplateConfigSchema } from '../types/provider.js';
    - import type { TemplateConfig, TemplateStore } from '../types/provider.js';
    - import { ValidationError } from '../types/validation.js';
    - import { z } from 'zod';
    - import path from 'path';

    在 src/lib/store/template.test.ts 编写测试：
    - 使用 vitest 的 describe/it/expect
    - 使用 beforeAll/afterAll 创建/清理临时测试目录
    - 每个测试创建新的 TemplateStore 实例指向临时目录
  </action>
  <verify>
    <automated>vitest run src/lib/store/template.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export class TemplateStore" src/lib/store/template.ts >= 1
    - grep -c "async getAll" src/lib/store/template.ts >= 1
    - grep -c "async get" src/lib/store/template.ts >= 1
    - grep -c "async set" src/lib/store/template.ts >= 1
    - grep -c "async delete" src/lib/store/template.ts >= 1
    - grep -c "async list" src/lib/store/template.ts >= 1
    - grep "import.*TemplateConfigSchema.*from.*provider.js" src/lib/store/template.ts
    - grep "getConfigDir" src/lib/store/template.ts
    - vitest run src/lib/store/template.test.ts 显示所有测试通过
  </acceptance_criteria>
  <done>
    TemplateStore 类完成，支持 getAll/get/set/delete/list 操作，测试覆盖率 >=80%。
  </done>
</task>

</tasks>

<verification>
- 所有 9 个测试通过
- TemplateStore 类符合 TemplateStoreSchema 结构
- 模板验证失败抛出 ValidationError
- set/delete 操作前创建备份
- 模板持久化到 templates.json
- 时间戳自动管理
</verification>

<success_criteria>
- [ ] getAll 返回所有模板
- [ ] get 按名称返回模板或 null
- [ ] set 创建/更新有效模板
- [ ] set 对无效模板抛出 ValidationError
- [ ] delete 删除模板并返回布尔结果
- [ ] list 返回模板名称数组
- [ ] 模板持久化正确
- [ ] 测试覆盖率 >= 80%
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-layer/03-02-SUMMARY.md`
</output>
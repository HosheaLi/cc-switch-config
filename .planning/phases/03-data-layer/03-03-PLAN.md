---
phase: 03-data-layer
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/store/project.ts, src/lib/store/project.test.ts]
autonomous: true
requirements: [DATA-03]
user_setup: []

must_haves:
  truths:
    - "ProjectIndex can register new projects"
    - "ProjectIndex can find projects by path"
    - "ProjectIndex can find projects by ID"
    - "ProjectIndex can update project metadata"
    - "ProjectIndex can remove projects"
    - "Projects persist across restarts"
  artifacts:
    - path: "src/lib/store/project.ts"
      provides: "ProjectIndex class for project metadata"
      min_lines: 120
      exports: ["ProjectIndex", "ProjectEntry", "ProjectIndexData"]
    - path: "src/lib/store/project.test.ts"
      provides: "Unit tests for ProjectIndex"
      min_lines: 100
    - path: "getDataDir()/projects.json"
      provides: "Project storage file"
  key_links:
    - from: "src/lib/store/project.ts"
      to: "src/lib/paths/xdg.ts"
      via: "getDataDir"
      pattern: "import.*getDataDir.*from.*xdg.js"
    - from: "src/lib/store/project.ts"
      to: "src/lib/file-system/json.ts"
      via: "readJSON/writeJSON"
      pattern: "import.*from.*json.js"
    - from: "src/lib/store/project.ts"
      to: "src/lib/file-system/backup.ts"
      via: "createBackup"
      pattern: "import.*createBackup.*from.*backup.js"
---

<objective>
创建 ProjectIndex 类，管理已注册项目的元数据（路径、激活配置、修改时间）。项目索引存储在 XDG data dir 的 projects.json 文件中。

Purpose: 提供项目注册、查找、更新、删除操作，支持按路径快速查找（pathIndex 副索引）。
Output: src/lib/store/project.ts (ProjectIndex 类) + 测试文件，项目索引存储于 getDataDir()/projects.json。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-layer/03-CONTEXT.md
@.planning/phases/03-data-layer/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/paths/xdg.ts:
```typescript
export function getConfigDir(): string;
export function getDataDir(): string;
```

From src/lib/file-system/json.ts:
```typescript
export async function readJSON<T = unknown>(filepath: string): Promise<T | null>;
export async function writeJSON(filepath: string, data: unknown): Promise<void>;
export async function exists(filepath: string): Promise<boolean>;
```

From src/lib/file-system/backup.ts:
```typescript
export async function createBackup(filepath: string): Promise<string | null>;
```

From crypto module (Node.js built-in):
```typescript
import { randomUUID } from 'crypto';
```

From fs module (Node.js built-in):
```typescript
import fs from 'fs-extra'; // provides realpath
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProjectIndex class</name>
  <files>src/lib/store/project.ts, src/lib/store/project.test.ts</files>
  <read_first>
    - src/lib/paths/xdg.ts (了解 getDataDir 函数)
    - src/lib/file-system/json.ts (了解 readJSON/writeJSON/exists)
    - src/lib/file-system/backup.ts (了解 createBackup)
    - src/lib/types/validation.ts (了解 ValidationError 类用于验证逻辑)
  </read_first>
  <behavior>
    - Test 1: register creates new project entry with UUID
    - Test 2: register returns existing entry if path already registered
    - Test 3: getByPath returns project entry for registered path
    - Test 4: getByPath returns null for non-existent path
    - Test 5: getById returns project entry for valid ID
    - Test 6: getById returns null for non-existent ID
    - Test 7: update modifies project metadata (activeConfig, lastModified)
    - Test 8: remove deletes project entry and returns true
    - Test 9: remove returns false for non-existent project
    - Test 10: getAll returns all project entries
    - Test 11: projects persist after index reload
    - Test 12: path normalization (realpath) handles symlinks
  </behavior>
  <action>
    在 src/lib/store/project.ts 实现 ProjectIndex 类：

    1. ProjectEntry 类型（per D-07）：
       ```typescript
       export interface ProjectEntry {
         id: string;           // UUID for stable reference
         path: string;         // Absolute path to project root (resolved)
         activeConfig: string | null; // Template name or null
         lastModified: string; // ISO timestamp
       }
       ```

    2. ProjectIndexData 类型：
       ```typescript
       export interface ProjectIndexData {
         version?: number;
         projects: Record<string, ProjectEntry>; // Keyed by ID
         pathIndex: Record<string, string>; // path -> ID for fast lookup
       }
       ```

    3. ProjectIndex 类：
       - 私有属性：filePath (getDataDir()/projects.json), data (缓存)
       - 私有方法 load(): Promise<ProjectIndexData>
       - 私有方法 save(): Promise<void>
       - 私有方法 normalizePath(p: string): Promise<string> — 使用 fs.realpath 解析为绝对路径

       公开方法：
       - async register(projectPath: string): Promise<ProjectEntry>
         - 规范化路径（realpath）
         - 检查是否已注册（pathIndex 查询）
         - 已注册则返回现有条目
         - 未注册则创建新条目（UUID, lastModified = now）
         - 备份后写入
       - async getByPath(projectPath: string): Promise<ProjectEntry | null>
         - 规范化路径
         - 通过 pathIndex 快速查找
       - async getById(id: string): Promise<ProjectEntry | null>
       - async update(id: string, updates: Partial<Pick<ProjectEntry, 'activeConfig' | 'lastModified'>>): Promise<boolean>
         - 更新指定字段
         - 更新 lastModified 时间戳
         - 备份后写入
       - async remove(id: string): Promise<boolean>
         - 删除条目和 pathIndex 映射
         - 备份后写入
       - async getAll(): Promise<ProjectEntry[]>

    4. 导出：
       ```typescript
       export class ProjectIndex { ... }
       export interface ProjectEntry { ... }
       export interface ProjectIndexData { ... }
       ```

    导入路径使用 .js 后缀：
    - import { readJSON, writeJSON, exists } from '../file-system/json.js';
    - import { createBackup } from '../file-system/backup.js';
    - import { getDataDir } from '../paths/xdg.js';
    - import { randomUUID } from 'crypto';
    - import fs from 'fs-extra';
    - import path from 'path';

    在 src/lib/store/project.test.ts 编写测试：
    - 使用 vitest 的 describe/it/expect
    - 使用 beforeAll/afterAll 创建/清理临时测试目录
    - 测试路径规范化：创建 symlink 验证 realpath 解析
  </action>
  <verify>
    <automated>vitest run src/lib/store/project.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export class ProjectIndex" src/lib/store/project.ts >= 1
    - grep -c "export interface ProjectEntry" src/lib/store/project.ts >= 1
    - grep -c "export interface ProjectIndexData" src/lib/store/project.ts >= 1
    - grep -c "async register" src/lib/store/project.ts >= 1
    - grep -c "async getByPath" src/lib/store/project.ts >= 1
    - grep -c "async getById" src/lib/store/project.ts >= 1
    - grep -c "async update" src/lib/store/project.ts >= 1
    - grep -c "async remove" src/lib/store/project.ts >= 1
    - grep "randomUUID" src/lib/store/project.ts
    - grep "getDataDir" src/lib/store/project.ts
    - grep "realpath" src/lib/store/project.ts
    - vitest run src/lib/store/project.test.ts 显示所有测试通过
  </acceptance_criteria>
  <done>
    ProjectIndex 类完成，支持 register/getByPath/getById/update/remove/getAll 操作，测试覆盖率 >=80%。
  </done>
</task>

</tasks>

<verification>
- 所有 12 个测试通过
- ProjectIndex 类使用 pathIndex 副索引快速查找
- 路径规范化使用 realpath
- UUID 作为稳定 ID
- 时间戳自动管理
- 项目持久化到 projects.json
</verification>

<success_criteria>
- [ ] register 创建新项目或返回已存在项目
- [ ] getByPath 通过路径快速查找
- [ ] getById 通过 ID 查找
- [ ] update 更新项目元数据
- [ ] remove 删除项目
- [ ] getAll 返回所有项目
- [ ] 路径规范化处理 symlinks
- [ ] 测试覆盖率 >= 80%
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-layer/03-03-SUMMARY.md`
</output>
---
phase: 08-quality-polish
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - scripts/benchmark.ts
  - typedoc.json
  - package.json
  - README.md
  - USAGE.md
  - docs/api/.gitkeep
autonomous: false
requirements:
  - N1
  - N2
  - N3
  - N4
  - D-09
user_setup:
  - service: typedoc
    why: API documentation generation
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - "Cold startup time < 1 second (N1)"
    - "Switch operation completes < 100ms (N2)"
    - "100 project scan completes < 5 seconds (N3)"
    - "TUI renders 100 items < 50ms (N4)"
    - "README provides quick start guide with installation steps"
    - "USAGE.md covers all CLI commands and TUI flows"
    - "API documentation generated from TypeScript sources"
  artifacts:
    - path: "scripts/benchmark.ts"
      provides: "Vitest benchmark tests for N1-N4"
      exports: []
      min_lines: 100
    - path: "README.md"
      provides: "Project documentation - quick start"
      contains: "## Installation"
      min_lines: 100
    - path: "USAGE.md"
      provides: "Detailed user guide for all features"
      contains: "## CLI Commands"
      min_lines: 200
    - path: "typedoc.json"
      provides: "TypeDoc configuration"
      contains: "entryPoints"
  key_links:
    - from: "scripts/benchmark.ts"
      to: "vitest bench mode"
      via: "bench() function from vitest"
      pattern: "bench"
    - from: "package.json"
      to: "scripts/benchmark.ts"
      via: "\"bench\": \"vitest bench scripts/benchmark.ts\""
      pattern: "bench"
---

<objective>
Implement performance benchmarks and complete project documentation.

Purpose: Validate N1-N4 performance targets and provide D-09 complete documentation.
Output: Benchmark script + README + USAGE.md + TypeDoc setup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-quality-polish/08-CONTEXT.md
@.planning/phases/08-quality-polish/08-RESEARCH.md
@.planning/phases/08-quality-polish/08-VALIDATION.md

<!-- Reference prior plans -->
@.planning/phases/08-quality-polish/08-04-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From RESEARCH.md (benchmark pattern):
```typescript
import { bench, describe } from 'vitest';
bench('cold startup time < 1s (N1)', async () => {
  // Measure service initialization
});
```

From package.json:
```json
"scripts": {
  "test": "vitest run",
  "test:coverage": "vitest run --coverage"
}
```

From REQUIREMENTS.md:
| N1 | Fast Startup | < 1 second cold start |
| N2 | Quick Operations | < 100ms for switch/list |
| N3 | Scalable Scanning | < 5s for 100 project scan |
| N4 | Responsive TUI | < 50ms render time |
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Performance Benchmark Script</name>
  <files>scripts/benchmark.ts, package.json</files>
  <read_first>
    - package.json (scripts section)
    - src/lib/services/project-service.ts (ProjectService for N1, N2, N3)
    - src/lib/services/template-service.ts (TemplateService for N2)
    - src/lib/store/state.ts (AppState for N1)
    - .planning/phases/08-quality-polish/08-RESEARCH.md (benchmark patterns)
    - .planning/phases/08-quality-polish/08-VALIDATION.md (acceptance criteria)
  </read_first>
  <behavior>
    - Test 1: N1 benchmark measures cold startup (service initialization)
    - Test 2: N2 benchmark measures switch operation time
    - Test 3: N3 benchmark measures 100 project scan time
    - Test 4: N4 benchmark measures TUI render for 100 items
    - Test 5: All benchmarks use vitest bench mode
    - Test 6: Benchmarks include mock data setup
  </behavior>
  <action>
    Create scripts/benchmark.ts with:

    1. **Install typedoc** (for Task 2):
       ```bash
       npm install typedoc --save-dev
       ```

    2. **N1: Cold Startup Benchmark**:
       ```typescript
       import { bench, describe, beforeAll } from 'vitest';
       import { ProjectService } from '../src/lib/services/project-service.js';
       import { AppState } from '../src/lib/store/state.js';
       import { ProjectIndex } from '../src/lib/store/project.js';
       
       describe('N1: Cold Startup < 1s', () => {
         bench('service initialization', async () => {
           const appState = new AppState();
           const projectIndex = new ProjectIndex();
           const projectService = new ProjectService(projectIndex, appState);
           // No explicit initialize - measure constructor + first access
         });
       });
       ```

    3. **N2: Switch Operation Benchmark**:
       ```typescript
       describe('N2: Switch Operation < 100ms', () => {
         beforeAll(async () => {
           // Setup mock project and template
         });
         
         bench('template apply', async () => {
           // Measure templateService.applyTemplate call
           // With mock filesystem
         });
       });
       ```

    4. **N3: 100 Project Scan Benchmark**:
       ```typescript
       describe('N3: 100 Project Scan < 5s', () => {
         beforeAll(async () => {
           // Create 100 mock project directories
         });
         
         bench('scan 100 projects', async () => {
           // Measure projectService.scanDirectory call
         });
       });
       ```

    5. **N4: TUI Render Benchmark**:
       ```typescript
       describe('N4: TUI Render 100 items < 50ms', () => {
         bench('render 100 project list items', async () => {
           // Use ink-testing-library render
           // Measure render time for ProjectListScreen with 100 items
         });
       });
       ```

    6. **Update package.json scripts**:
       ```json
       "scripts": {
         "bench": "vitest bench scripts/benchmark.ts",
         "docs": "typedoc --out docs/api src"
       }
       ```

    7. **Mock data approach**:
       - Use vitest's mock capabilities
       - Create temporary test directories for scan
       - Use in-memory data for other benchmarks

    Per N1-N4: Performance targets validated by benchmarks.
    Per VALIDATION.md: Acceptance criteria with 90th percentile.
  </action>
  <verify>
    <automated>npm run bench</automated>
  </verify>
  <acceptance_criteria>
    - scripts/benchmark.ts exists
    - Benchmark file imports bench, describe from 'vitest'
    - Benchmark file contains N1 cold startup test
    - Benchmark file contains N2 switch operation test
    - Benchmark file contains N3 100 project scan test
    - Benchmark file contains N4 TUI render test
    - package.json contains "bench" script
    - package.json contains typedoc in devDependencies
    - npm run bench executes successfully (even if some tests are mocked placeholders)
  </acceptance_criteria>
  <done>
    Benchmark script validates N1-N4 performance targets.
    Vitest bench mode configured.
    Typedoc dependency installed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Documentation Review and Completion</name>
  <files>README.md, USAGE.md, typedoc.json</files>
  <read_first>
    - README.md (existing - may need update)
    - package.json (for project info)
    - .planning/REQUIREMENTS.md (for feature descriptions)
    - .planning/ROADMAP.md (for phase summary)
    - src/cli/index.ts (for CLI commands reference)
    - src/tui/screens/ (for TUI flows)
  </read_first>
  <action>
    This task requires human review of documentation content.

    **Claude preparation work:**

    1. **Check if README.md exists** and read current content.

    2. **Prepare README.md structure** (if missing or incomplete):
       - Title: CCAPISwitch - CLI/TUI Configuration Management
       - Description: Manage Claude Code project-level API provider configurations
       - ## Installation: npm install cc-config-switch
       - ## Quick Start: cc-config (launches TUI)
       - ## CLI Commands: list, switch, current, template, undo, scan, auto-check, import, export
       - ## Features: Profile CRUD, Interactive TUI, Templates, Validation, Undo, Import/Export
       - ## Development: npm run dev, npm test, npm run build

    3. **Create USAGE.md** with detailed guide:
       - ## Overview
       - ## CLI Commands (detailed with examples)
         - cc-config list [--json]
         - cc-config switch [template] [--silent]
         - cc-config current
         - cc-config template list/create/delete
         - cc-config undo
         - cc-config scan [directory]
         - cc-config import/export
         - cc-config auto-check
       - ## TUI Navigation
         - Starting the TUI
         - Project selection (arrow keys, j/k, fuzzy search)
         - Template preview and application
         - Diff view before apply
         - Validation error handling
         - Undo with 'U' key
       - ## Configuration
         - Template format
         - Project registration
         - Auto-switch hooks
       - ## Troubleshooting

    4. **Create typedoc.json** configuration:
       ```json
       {
         "entryPoints": ["src/lib", "src/cli", "src/tui"],
         "out": "docs/api",
         "exclude": ["**/*.test.ts", "**/*.test.tsx"],
         "plugin": [],
         "theme": "default"
       }
       ```

    5. **Update package.json docs script** (if needed):
       ```json
       "docs": "typedoc"
       ```

    **Checkpoint asks user to:**
    - Review README.md content
    - Review USAGE.md coverage
    - Verify all features documented
    - Approve TypeDoc configuration
  </action>
  <verify>
    <automated>MISSING - documentation quality is manual review</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "## Installation" section
    - README.md contains "## Quick Start" section
    - README.md contains "## CLI Commands" section
    - USAGE.md exists
    - USAGE.md contains "## CLI Commands" section
    - USAGE.md contains "## TUI Navigation" section
    - typedoc.json exists with entryPoints configured
    - docs/api/.gitkeep exists (placeholder for generated docs)
  </acceptance_criteria>
  <what_built>
    Documentation files created/updated:
    - README.md with quick start guide
    - USAGE.md with detailed user guide
    - typedoc.json for API documentation generation
  </what_built>
  <how_to_verify>
    1. Review README.md sections: Installation, Quick Start, CLI Commands
    2. Review USAGE.md coverage: all CLI commands and TUI flows documented
    3. Verify typedoc.json configuration includes src/lib, src/cli, src/tui
    4. Check package.json has "docs" script
    5. Optionally run `npm run docs` to verify TypeDoc generates output
  </how_to_verify>
  <resume_signal>Type "approved" or describe documentation issues to fix</resume_signal>
  <done>
    Documentation complete with README, USAGE guide, and TypeDoc setup.
    User reviewed and approved content.
  </done>
</task>

</tasks>

<verification>
After Task 1 complete:
- Run: `npm run bench` (benchmarks execute)
- Verify: Typedoc dependency installed

After Task 2 checkpoint:
- User approves documentation
- README.md has required sections
- USAGE.md covers all features
- typedoc.json configured
</verification>

<success_criteria>
- Performance benchmarks validate N1-N4 targets
- README.md provides clear quick start
- USAGE.md covers all CLI commands and TUI flows
- TypeDoc configured for API docs
- User approved documentation
</success_criteria>

<output>
After completion, create `.planning/phases/08-quality-polish/08-05-SUMMARY.md`
</output>
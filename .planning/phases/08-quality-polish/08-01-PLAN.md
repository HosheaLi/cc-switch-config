---
phase: 08-quality-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/utils/diff.ts
  - src/cli/utils/diff.test.ts
  - src/tui/components/UnifiedDiff.tsx
  - src/tui/components/UnifiedDiff.test.tsx
autonomous: true
requirements:
  - F12
must_haves:
  truths:
    - "User can see unified diff showing what will change before applying template"
    - "Diff displays only changed fields, not entire config"
    - "Removed lines appear in red, added lines appear in green"
    - "Diff format matches git-style: - before, + after"
  artifacts:
    - path: "src/cli/utils/diff.ts"
      provides: "Diff generation utilities (generateUnifiedDiff, filterChangedFields)"
      exports: ["generateUnifiedDiff", "filterChangedFields", "DiffLine"]
      min_lines: 50
    - path: "src/tui/components/UnifiedDiff.tsx"
      provides: "React component for rendering unified diff lines"
      exports: ["UnifiedDiff", "DiffLine"]
      min_lines: 40
  key_links:
    - from: "src/tui/screens/DiffScreen.tsx"
      to: "src/cli/utils/diff.ts"
      via: "import { generateUnifiedDiff } from '../../cli/utils/diff.js'"
      pattern: "generateUnifiedDiff"
    - from: "src/tui/screens/DiffScreen.tsx"
      to: "src/tui/components/UnifiedDiff.tsx"
      via: "import { UnifiedDiff } from '../components/UnifiedDiff.js'"
      pattern: "UnifiedDiff"
---

<objective>
Implement diff generation utilities and rendering component for unified diff display.

Purpose: Provide the foundation for F12 (Diff Before Apply) - generate and display git-style unified diffs.
Output: Diff utility functions + UnifiedDiff Ink component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-quality-polish/08-CONTEXT.md
@.planning/phases/08-quality-polish/08-RESEARCH.md
@.planning/phases/08-quality-polish/08-UI-SPEC.md
@.planning/phases/08-quality-polish/08-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/types/config.ts (ClaudeSettings):
```typescript
export interface ClaudeSettings {
  env?: Record<string, string>;
  permissions?: PermissionRule[];
  hooks?: HookConfig[];
  mcpServers?: Record<string, McpServerConfig>;
  apiProvider?: ApiProviderConfig[];
}
```

From src/lib/types/merge.ts (deepMergeConfig pattern):
```typescript
export function deepMergeConfig(base: ClaudeSettings, override: Partial<ClaudeSettings>): ClaudeSettings;
```

From UI-SPEC.md (DiffLine interface):
```typescript
interface DiffLine {
  type: 'added' | 'removed' | 'modified';
  path: string;
  value?: unknown;       // for added/removed
  before?: unknown;      // for modified
  after?: unknown;       // for modified
}
```

From RESEARCH.md (diff package usage):
```typescript
import * as Diff from 'diff';
const diffResult = Diff.createPatch('settings.json', beforeJson, afterJson);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Diff Generation Utilities</name>
  <files>src/cli/utils/diff.ts, src/cli/utils/diff.test.ts</files>
  <read_first>
    - src/lib/types/config.ts (ClaudeSettings type)
    - src/lib/types/merge.ts (deepMergeConfig pattern for comparison)
    - src/lib/types/provider.ts (TemplateConfig type)
    - src/cli/utils/index.ts (barrel export pattern)
    - package.json (diff package not yet installed)
  </read_first>
  <behavior>
    - Test 1: generateUnifiedDiff(before, after) returns DiffLine[] with 'removed' type for deleted fields
    - Test 2: generateUnifiedDiff(before, after) returns DiffLine[] with 'added' type for new fields
    - Test 3: generateUnifiedDiff(before, after) returns DiffLine[] with 'modified' type for changed values
    - Test 4: filterChangedFields returns only paths that differ between before/after
    - Test 5: Empty diff when before and after are identical
    - Test 6: Nested field paths use dot notation (e.g., "env.MODEL")
  </behavior>
  <action>
    Install diff package: `npm install diff --save-dev`

    Create src/cli/utils/diff.ts with:
    
    1. **DiffLine interface** (per UI-SPEC.md):
       ```typescript
       export interface DiffLine {
         type: 'added' | 'removed' | 'modified';
         path: string;
         value?: unknown;
         before?: unknown;
         after?: unknown;
       }
       ```

    2. **generateUnifiedDiff function** (per D-01, D-02):
       - Parameters: before (ClaudeSettings), after (ClaudeSettings)
       - Returns: DiffLine[] sorted by path
       - Uses deep-object-diff or custom comparison to detect changes
       - For modified fields: two entries (removed + added) or single 'modified' entry
       - Only include fields that actually changed (D-02)
       - Nested paths use dot notation: "env.MODEL", "apiProvider[0].baseUrl"

    3. **filterChangedFields function**:
       - Parameters: before, after
       - Returns: string[] of paths that differ
       - Helper for generateUnifiedDiff

    4. **Implementation approach** (from RESEARCH.md):
       - Use deep-object-diff package for object comparison
       - Install: `npm install deep-object-diff --save-dev`
       - Import: `import { diff as deepDiff } from 'deep-object-diff'`
       - Convert deep-object-diff output to DiffLine format

    5. **Add to barrel export** in src/cli/utils/index.ts:
       - Export generateUnifiedDiff, filterChangedFields, DiffLine

    Per D-01: Unified diff format with red/green colors (colors applied in component, not here).
    Per D-02: Only show changed fields, not entire config.
  </action>
  <verify>
    <automated>vitest run src/cli/utils/diff.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/utils/diff.ts exists with generateUnifiedDiff function exported
    - src/cli/utils/diff.test.ts exists with at least 6 test cases
    - Test file contains test for "generateUnifiedDiff returns DiffLine[] with 'removed' type"
    - Test file contains test for "filterChangedFields returns only paths that differ"
    - src/cli/utils/index.ts exports generateUnifiedDiff, filterChangedFields, DiffLine
    - package.json contains "diff" in devDependencies
    - package.json contains "deep-object-diff" in devDependencies
  </acceptance_criteria>
  <done>
    Diff utilities generate DiffLine[] from before/after configs.
    Tests pass covering all diff types (added, removed, modified).
    Barrel export includes all diff utilities.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UnifiedDiff Ink Component</name>
  <files>src/tui/components/UnifiedDiff.tsx, src/tui/components/UnifiedDiff.test.tsx</files>
  <read_first>
    - src/tui/components/PreviewPanel.tsx (component structure pattern)
    - src/tui/components/StatusBar.tsx (Text color usage pattern)
    - src/tui/screens/ConfirmScreen.tsx (Box border + padding pattern)
    - src/cli/utils/diff.ts (DiffLine interface - created in Task 1)
    - src/tui/components/index.ts (barrel export pattern)
  </read_first>
  <behavior>
    - Test 1: UnifiedDiff renders 'removed' lines with red color
    - Test 2: UnifiedDiff renders 'added' lines with green color
    - Test 3: UnifiedDiff renders 'modified' lines as two lines (red before, green after)
    - Test 4: UnifiedDiff renders path: value format correctly
    - Test 5: Empty diffLines array renders "No changes detected" message
    - Test 6: Component accepts DiffLine[] prop and renders all lines
  </behavior>
  <action>
    Create src/tui/components/UnifiedDiff.tsx with:

    1. **Props interface**:
       ```typescript
       interface UnifiedDiffProps {
         lines: DiffLine[];
         beforeLabel?: string;  // default: '--- settings.json (before)'
         afterLabel?: string;   // default: '+++ settings.json (after)'
       }
       ```

    2. **Component structure** (per UI-SPEC.md):
       - Container: `<Box flexDirection="column">`
       - Header labels: beforeLabel (dimColor), afterLabel (dimColor)
       - Each removed line: `<Text color="red">- {path}: {JSON.stringify(value)}</Text>`
       - Each added line: `<Text color="green">+ {path}: {JSON.stringify(value)}</Text>`
       - Modified fields: Two consecutive Text elements (red `-` then green `+`)
       - Empty state: `<Text dimColor>No changes detected — config unchanged</Text>`

    3. **Color mapping** (per D-01, UI-SPEC.md):
       - `color="red"` for removed/before values
       - `color="green"` for added/after values
       - Use Ink's Text color prop (not chalk - Ink handles terminal colors)

    4. **Value formatting**:
       - Use JSON.stringify for object/array values
       - String values displayed directly (no quotes for readability)
       - null/undefined shown as "null"/"undefined"

    5. **Add to barrel export** in src/tui/components/index.ts:
       - Export UnifiedDiff component

    Per UI-SPEC.md Color Contract: red for removed, green for added.
    Per UI-SPEC.md Copywriting: "- {path}: {value}" format.
  </action>
  <verify>
    <automated>vitest run src/tui/components/UnifiedDiff.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/tui/components/UnifiedDiff.tsx exists
    - src/tui/components/UnifiedDiff.test.tsx exists with at least 6 test cases
    - Component file contains "color=\"red\"" for removed lines
    - Component file contains "color=\"green\"" for added lines
    - Component file imports DiffLine from '../../cli/utils/diff.js'
    - src/tui/components/index.ts exports UnifiedDiff
    - Test file contains test for "renders removed lines with red color"
    - Test file contains test for "renders added lines with green color"
  </acceptance_criteria>
  <done>
    UnifiedDiff component renders DiffLine[] with proper colors.
    Tests pass covering all rendering cases.
    Barrel export includes UnifiedDiff.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- Run: `vitest run src/cli/utils/diff.test.ts src/tui/components/UnifiedDiff.test.tsx`
- Verify: Both test files pass
- Verify: Barrel exports updated
- Verify: package.json has diff and deep-object-diff dependencies
</verification>

<success_criteria>
- Diff utilities generate unified diff lines from config comparison
- UnifiedDiff component renders diffs with red/green colors
- All tests pass (12+ test cases total)
- Dependencies installed: diff, deep-object-diff
</success_criteria>

<output>
After completion, create `.planning/phases/08-quality-polish/08-01-SUMMARY.md`
</output>
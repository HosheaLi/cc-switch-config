---
wave: 4
depends_on:
  - 15-02
  - 15-03
  - 15-04
files_modified:
  - src/lib/types/provider.ts (最终清理)
  - src/cli/commands/ (index.ts 注册——删除 template 命令)
  - package.json (最终验证)
requirements:
  - TUI-06
  - CFG-06
autonomous: true
---

# Plan 05: Final Cleanup and Verification

## Objective

执行最终验证——确保 Ink/React 完全移除、Template* 完全移除、chalk 完全移除、bundle 体积减小。清理 provider.ts 中残留的 Template* 类型定义，移除 template 命令注册，验证构建产物。

## Background

Plans 01-04 执行了所有删除和迁移。本计划做最终清理和全量验证，确保 Phase 15 成功标准全部达成。

## Tasks

### Task 01: Final cleanup of provider.ts

<read_first>
- src/lib/types/provider.ts
- src/lib/types/index.ts
</read_first>

<action>
1. 检查 `src/lib/types/provider.ts`，删除所有 Template* 相关的 Zod schema 和类型定义（Plan 02 Task 02 可能因消费者依赖而保留了部分）
2. 如果 `provider.ts` 删除所有内容后为空文件，删除该文件并从 `src/lib/types/index.ts` 移除 barrel export
3. 如果 `provider.ts` 仍有 `ProviderConfig` 等非 Template 类型在使用，保留这些类型
</action>

<acceptance_criteria>
- `grep "TemplateConfig\|TemplateStore\|TemplateConfigSchema\|TemplateStoreSchema" src/lib/types/provider.ts` 返回空
- 如果文件为空则 `test -f src/lib/types/provider.ts` 返回非零
- `grep "template" src/lib/types/index.ts` 返回空（无 provider.ts barrel export 如果文件被删除）
</acceptance_criteria>

### Task 02: Remove template command registration

<read_first>
- src/cli/index.ts (commander 注册)
</read_first>

<action>
1. 从 `src/cli/index.ts` 中移除 template 命令的注册（如 `program.addCommand(templateCommand)`）
2. 删除 `import templateCommand from './commands/template.js'`（如果还存在）
3. template 命令已被 `cc-config config add/list/remove` 完全替代（CFG-03），无需保留为 alias
</action>

<acceptance_criteria>
- `grep "template" src/cli/index.ts` 仅出现在无关上下文中（如 "template" 字面字符串），不包含命令注册
- `grep "templateCommand\|addCommand.*template" src/cli/index.ts` 返回空
</acceptance_criteria>

### Task 03: Full verification against Phase 15 success criteria

<read_first>
- package.json
- tsconfig.json
</read_first>

<action>
按 ROADMAP 5 条成功标准逐项验证：

1. **"Ink React TUI layer completely removed from dependencies"**
   ```bash
   grep -E '"ink"|"react"' package.json  # 必须返回空
   test ! -d src/tui  # 必须成功
   ```

2. **"TemplateConfig/TemplateService/TemplateStore removed and replaced with simplified config service"**
   ```bash
   grep -r "TemplateService\|TemplateStore\|TemplateConfig" src/lib/ --include="*.ts" -l  # 必须返回空
   grep "applyTemplate" src/lib/services/config-service.ts  # 必须返回空
   grep "applyApiConfig" src/lib/services/config-service.ts  # 必须返回非空
   ```

3. **"All Ink components replaced with prompts equivalents"**
   ```bash
   grep -r "from 'ink'" src/ --include="*.ts" -l  # 必须返回空
   grep -r "ink-testing-library" src/ --include="*.ts" -l  # 必须返回空
   ```

4. **"No React dependencies remain in TUI layer"**
   ```bash
   grep -r "from 'react'" src/ --include="*.ts" -l  # 必须返回空
   grep -r "react" package.json  # 必须返回空（无 @types/react 也无 react）
   ```

5. **"Bundle size reduced without React/Ink overhead"**
   ```bash
   npm run build  # 或 npx tsup
   ls -la dist/  # 记录大小
   ```

最终测试：
```bash
npx tsc --noEmit
npx vitest run --reporter=verbose
```
</action>

<acceptance_criteria>
- `grep -E '"ink"|"react"|"@types/react"|"@testing-library/react"|"ink-testing-library"' package.json` 返回空
- `grep -r "TemplateService\|TemplateStore\|TemplateConfig" src/lib/ src/cli/ --include="*.ts" -l` 返回空
- `grep -r "from 'ink'\|from 'react'\|from 'chalk'" src/ --include="*.ts" -l` 返回空
- `test ! -d src/tui` 成功
- `npx tsc --noEmit` 通过
- `npx vitest run --reporter=verbose` 通过
- `npm run build` 成功
</acceptance_criteria>

## Verification

```bash
npx tsc --noEmit
npx vitest run --coverage
npm run build
```

## must_haves

- 5 条 ROADMAP 成功标准全部满足
- TypeScript 编译通过
- 全量测试通过
- 构建成功
- package.json 不再包含 ink/react/chalk 相关依赖
- src/tui/ 目录不存在
- src/ 中零 Template*/Ink/chalk 引用

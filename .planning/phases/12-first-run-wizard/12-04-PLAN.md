---
phase: 12-first-run-wizard
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [ONB-05]
user_setup: []

must_haves:
  truths:
    - "User sees animated spinner during scan operation"
    - "Spinner shows Unicode glyphs cycling at 80ms intervals"
    - "Spinner succeed message shows discovered project count"
    - "Spinner fail message shows error description"
  artifacts:
    - path: "src/cli/prompts/wizards/main-wizard.ts"
      provides: "createSpinner function"
      contains: "function createSpinner"
      min_lines: 24
  key_links:
    - from: "main-wizard.ts L113"
      to: "createSpinner"
      via: "spinner = createSpinner('扫描中...')"
      pattern: "createSpinner\\("
    - from: "main-wizard.ts L115"
      to: "spinner.succeed"
      via: "spinner.succeed(`扫描完成: ${results.length} 个项目`)"
      pattern: "spinner\\.succeed"
---

<objective>
Verify that the spinner progress indicator displays correctly during the first-run wizard scan operation. This is a visual/interaction verification checkpoint for ONB-05.

Purpose: Ensure users see proper progress feedback during project scanning, matching the terminal-native design aesthetic.
Output: Verified spinner behavior (visual confirmation, no code changes).
</objective>

<execution_context>
@/Users/lihaoxuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lihaoxuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-first-run-wizard/12-CONTEXT.md
@.planning/phases/12-first-run-wizard/12-RESEARCH.md
@.planning/phases/12-first-run-wizard/12-UI-SPEC.md

<interfaces>
<!-- Key interfaces from existing implementation -->

From src/cli/prompts/wizards/main-wizard.ts (L22-45):
```typescript
function createSpinner(message: string) {
  let frame = 0;
  const frames = ['⠋', '⠙', '⠹', '⠸', '⠼', '⠴', '⠦', '⠧', '⠇', '⠏'];

  const interval = setInterval(() => {
    process.stdout.write(`\r${frames[frame]} ${message}`);
    frame = (frame + 1) % frames.length;
  }, 80);

  return {
    succeed: (msg: string) => {
      clearInterval(interval);
      process.stdout.write(`\r${chalk.green('✓')} ${msg}\n`);
    },
    fail: (msg: string) => {
      clearInterval(interval);
      process.stdout.write(`\r${chalk.red('✗')} ${msg}\n`);
    },
    stop: () => {
      clearInterval(interval);
      process.stdout.write('\r' + ' '.repeat(60) + '\r');
    },
  };
}
```

From src/cli/prompts/wizards/main-wizard.ts (L113-115) - Usage:
```typescript
const spinner = createSpinner('扫描中...');
const results = await projectService.scanProjects(undefined, [directory]);
spinner.succeed(`扫描完成: ${results.length} 个项目`);
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify spinner visual display during wizard scan</name>
  <files></files>
  <read_first>
    <file>src/cli/prompts/wizards/main-wizard.ts</file>
  </read_first>
  <action>
  **Automated setup completed by Claude:**

  1. Claude has verified the spinner implementation exists at main-wizard.ts L22-45
  2. Claude has verified the spinner usage at main-wizard.ts L113-115
  3. Claude has verified the Unicode glyph frames: ['⠋', '⠙', '⠹', '⠸', '⠼', '⠴', '⠦', '⠧', '⠇', '⠏']
  4. Claude has verified the frame rate: 80ms interval
  5. Claude has verified the succeed/fail message formatting with chalk green/red

  **Human verification required:**

  User must visually confirm the spinner animation displays correctly in a real terminal environment. This cannot be automated because:
  - Unicode glyph rendering depends on terminal font support
  - Animation smoothness depends on terminal rendering speed
  - Color display depends on terminal color support and NO_COLOR environment variable
  </action>
  <how_to_verify>
    **Step-by-step verification procedure:**

    1. **Prepare test environment:**
       - Open a terminal that supports Unicode (most modern terminals: iTerm2, Terminal.app, VS Code terminal, Windows Terminal)
       - Ensure terminal width >= 60 chars for spinner clear space

    2. **Trigger first-run wizard:**
       - Run: `cd /Users/lihaoxuan/code/P07_CCAPISwitch`
       - Clear existing state to simulate first-run: `rm -rf ~/.config/cc-config-node/*` (optional, if you want clean state)
       - Run: `node dist/cli/index.js` (or `npm run build && node dist/cli/index.js` if not built)

    3. **Observe spinner during scan:**
       - Navigate through wizard steps until scan begins
       - **Expected visual:** Animated Unicode glyphs cycling: ⠋ → ⠙ → ⠹ → ⠸ → ⠼ → ⠴ → ⠦ → ⠧ → ⠇ → ⠏
       - **Expected message:** "扫描中..." displayed after glyph
       - **Expected animation speed:** Smooth 80ms frame transitions (approximately 12.5 frames per second)

    4. **Verify completion message:**
       - After scan completes, spinner should stop and show: `✓ 扫描完成: N 个项目`
       - Green checkmark (✓) with chalk.green styling
       - N should be the actual count of discovered projects

    5. **Test NO_COLOR support (optional):**
       - Run: `NO_COLOR=1 node dist/cli/index.js`
       - Verify spinner still works but without color styling
       - Success message should be plain text without green color

    6. **Test error handling (optional):**
       - If scan fails (permission error, etc.), verify: `✗ 扫描失败: {error message}`
       - Red X (✗) with chalk.red styling

    **Accept visual behavior:**
    - Glyphs cycle smoothly without stuttering
    - Message text is readable
    - Completion message shows correct project count
    - Colors display correctly (green for success, red for failure)
    - NO_COLOR environment variable is respected
  </how_to_verify>
  <acceptance_criteria>
    - User confirms glyphs animate smoothly in their terminal
    - User confirms "扫描中..." message appears after glyph
    - User confirms completion message shows "✓ 扫描完成: N 个项目" with correct count
    - User confirms colors display correctly (or respect NO_COLOR if set)
    - User confirms no visual glitches or overlapping text
  </acceptance_criteria>
  <verify>
    <automated>Manual verification — user types "approved" after visual confirmation</automated>
    <manual_steps>
      1. Build project: npm run build
      2. Run wizard: node dist/cli/index.js
      3. Observe spinner animation during scan
      4. Verify completion message format
      5. Type "approved" to confirm
    </manual_steps>
  </verify>
  <done>User typed "approved" confirming spinner visual display works correctly in terminal</done>
  <resume-signal>Type "approved" to confirm spinner works correctly, or describe visual issues encountered</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terminal output | Spinner writes to process.stdout (local terminal, trusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Information Disclosure | Spinner output | accept | Spinner displays only progress state, no sensitive data (API keys masked elsewhere per SEC-01) |
| T-12-04-02 | Tampering | Spinner interval | accept | 80ms interval is cosmetic, tampering would only affect visual smoothness (no security impact) |

**Security Notes:**
- This checkpoint is pure visual verification with no security implications
- Spinner does not expose any sensitive information (API keys, paths, etc.)
- All security mechanisms (SEC-01/SEC-04) verified in prior plans
</threat_model>

<verification>
- Visual confirmation by user (cannot be automated)
- Code structure verified via grep: `grep -n "createSpinner" src/cli/prompts/wizards/main-wizard.ts`
- Implementation verified: Unicode frames array exists, setInterval at 80ms, succeed/fail methods present
</verification>

<success_criteria>
- User visually confirms spinner animation works correctly in their terminal
- User confirms completion message displays with correct project count
- User confirms NO_COLOR environment variable is respected (optional test)
- No visual issues reported (stuttering, overlapping, missing glyphs)
</success_criteria>

<output>
After completion, create `.planning/phases/12-first-run-wizard/12-04-SUMMARY.md`
</output>
---
phase: 05-cli-interface
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [package.json, src/cli/output/error.ts, src/cli/output/error.test.ts, src/cli/index.test.ts, src/cli/commands/list.test.ts, src/cli/commands/switch.test.ts, src/cli/commands/current.test.ts, src/cli/commands/template.test.ts, src/cli/output/table.test.ts, src/cli/utils/tui-launch.test.ts]
autonomous: true
requirements: [U1]
user_setup: []
must_haves:
  truths:
    - "CLI errors display with clear messages and exit codes"
    - "Test infrastructure exists for all CLI modules"
    - "cli-table3 dependency available for table formatting"
  artifacts:
    - path: "src/cli/output/error.ts"
      provides: "ExitCodes constants and handleCLIError function"
      min_lines: 30
      exports: ["ExitCodes", "handleCLIError"]
    - path: "src/cli/output/error.test.ts"
      provides: "Error handling unit tests"
      contains: "describe('handleCLIError')"
    - path: "src/cli/index.test.ts"
      provides: "CLI entry point test stub"
      contains: "describe('CLI entry point')"
  key_links:
    - from: "src/cli/output/error.ts"
      to: "src/lib/services/types.ts"
      via: "ServiceError import"
      pattern: "import.*ServiceError"
---

<objective>
建立 CLI 测试基础设施和错误处理模块，为后续命令实现提供测试骨架和统一的错误输出机制。

Purpose: Wave 0 必须先创建测试文件，后续实现才能遵循 TDD-Strict 原则。错误处理模块统一 CLI 输出格式，符合 U1 要求。
Output: cli-table3 依赖安装、错误处理模块、全部 CLI 测试骨架。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cli-interface/05-CONTEXT.md
@.planning/phases/05-cli-interface/05-RESEARCH.md
@.planning/phases/05-cli-interface/05-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/services/types.ts:
```typescript
export class ServiceError extends Error {
  public readonly code: string;
  public readonly context?: Record<string, unknown>;
  constructor(message: string, code: string, context?: Record<string, unknown>);
}
```

From package.json:
```json
{
  "dependencies": {
    "chalk": "5.6.2",
    "commander": "14.0.3"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install cli-table3 and create test stub files</name>
  <files>package.json, src/cli/index.test.ts, src/cli/commands/list.test.ts, src/cli/commands/switch.test.ts, src/cli/commands/current.test.ts, src/cli/commands/template.test.ts, src/cli/output/table.test.ts, src/cli/utils/tui-launch.test.ts</files>
  <read_first>
    - package.json (check existing dependencies)
  </read_first>
  <behavior>
    - Test: npm install completes without errors
    - Test: All test stub files exist and contain describe blocks
    - Test: vitest run src/cli/ exits with 0 (tests are stubs, no failures)
  </behavior>
  <action>
    1. Install cli-table3 dependency: `npm install cli-table3@0.6.5`
    
    2. Create test stub files with minimal structure:
    
    **src/cli/index.test.ts** (per VALIDATION.md Wave 0):
    ```typescript
    import { describe, it, expect } from 'vitest';
    
    describe('CLI entry point', () => {
      it('placeholder - Wave 0 stub', () => {
        expect(true).toBe(true);
      });
    });
    ```
    
    **src/cli/commands/list.test.ts**:
    ```typescript
    import { describe, it, expect } from 'vitest';
    
    describe('list command', () => {
      it('placeholder - Wave 0 stub', () => {
        expect(true).toBe(true);
      });
    });
    ```
    
    **src/cli/commands/switch.test.ts**:
    ```typescript
    import { describe, it, expect } from 'vitest';
    
    describe('switch command', () => {
      it('placeholder - Wave 0 stub', () => {
        expect(true).toBe(true);
      });
    });
    ```
    
    **src/cli/commands/current.test.ts**:
    ```typescript
    import { describe, it, expect } from 'vitest';
    
    describe('current command', () => {
      it('placeholder - Wave 0 stub', () => {
        expect(true).toBe(true);
      });
    });
    ```
    
    **src/cli/commands/template.test.ts**:
    ```typescript
    import { describe, it, expect } from 'vitest';
    
    describe('template command', () => {
      it('placeholder - Wave 0 stub', () => {
        expect(true).toBe(true);
      });
    });
    ```
    
    **src/cli/output/table.test.ts**:
    ```typescript
    import { describe, it, expect } from 'vitest';
    
    describe('table formatter', () => {
      it('placeholder - Wave 0 stub', () => {
        expect(true).toBe(true);
      });
    });
    ```
    
    **src/cli/utils/tui-launch.test.ts**:
    ```typescript
    import { describe, it, expect } from 'vitest';
    
    describe('TUI launch utility', () => {
      it('placeholder - Wave 0 stub', () => {
        expect(true).toBe(true);
      });
    });
    ```
    
    These stubs allow vitest to run without failures, enabling Wave 1 to implement real tests incrementally.
  </action>
  <verify>
    <automated>npm test -- --filter=src/cli 2>&1 | grep -E "(passed|PASS)" && echo "Test stubs verified"</automated>
  </verify>
  <done>
    - cli-table3@0.6.5 appears in package.json dependencies
    - All 7 test stub files exist with describe blocks
    - npm test passes with stub tests
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create error handling module with ExitCodes and handleCLIError</name>
  <files>src/cli/output/error.ts, src/cli/output/error.test.ts</files>
  <read_first>
    - src/lib/services/types.ts (ServiceError class definition)
    - package.json (chalk version)
  </read_first>
  <behavior>
    - Test: handleCLIError(ServiceError) outputs colored message to stderr
    - Test: handleCLIError(ServiceError) exits with mapped exit code
    - Test: ExitCodes.SUCCESS equals 0
    - Test: ExitCodes.NOT_FOUND equals 3
  </behavior>
  <action>
    Create **src/cli/output/error.ts** implementing U1 (Clear Errors) and D-03 (stderr + exit code + color):
    
    ```typescript
    /**
     * CLI Error Handling Module
     *
     * Per U1: Clear errors with helpful messages.
     * Per D-03: stderr output + exit code + colored messages via chalk.
     *
     * Provides:
     * - ExitCodes: Standard exit code constants
     * - handleCLIError: Unified error output with chalk coloring
     * - mapErrorToExitCode: ServiceError.code → exit code mapping
     */
    
    import chalk from 'chalk';
    import { ServiceError } from '../../lib/services/types.js';
    
    /**
     * Standard CLI exit codes.
     * Follows Unix conventions where 0 = success, non-zero = error.
     */
    export const ExitCodes = {
      /** Successful completion */
      SUCCESS: 0,
      /** General/unspecified error */
      GENERAL_ERROR: 1,
      /** Misuse of command (invalid arguments) */
      MISUSE: 2,
      /** Resource not found (template/project/config) */
      NOT_FOUND: 3,
      /** Configuration validation error */
      CONFIG_ERROR: 4,
    } as const;
    
    /**
     * Handle CLI error with colored output and exit code.
     * Per D-03: Outputs to stderr, uses chalk for colors, exits with appropriate code.
     *
     * @param error - Error to handle (ServiceError or generic Error)
     * @param code - Optional override exit code (default: derived from error)
     */
    export function handleCLIError(error: unknown, code?: number): void {
      if (error instanceof ServiceError) {
        // ServiceError: display code + message, map to exit code
        console.error(chalk.red(`[${error.code}] ${error.message}`));
        const exitCode = code ?? mapErrorToExitCode(error.code);
        process.exit(exitCode);
      } else if (error instanceof Error) {
        // Generic Error: display message only
        console.error(chalk.red(`Error: ${error.message}`));
        process.exit(code ?? ExitCodes.GENERAL_ERROR);
      } else {
        // Unknown error type
        console.error(chalk.red('Unknown error occurred'));
        process.exit(ExitCodes.GENERAL_ERROR);
      }
    }
    
    /**
     * Map ServiceError.code to CLI exit code.
     *
     * @param code - ServiceError code string
     * @returns Corresponding CLI exit code
     */
    function mapErrorToExitCode(code: string): number {
      const codeMap: Record<string, number> = {
        // Template errors
        TEMPLATE_NOT_FOUND: ExitCodes.NOT_FOUND,
        TEMPLATE_CREATE_FAILED: ExitCodes.GENERAL_ERROR,
        TEMPLATE_UPDATE_FAILED: ExitCodes.GENERAL_ERROR,
        TEMPLATE_APPLY_FAILED: ExitCodes.CONFIG_ERROR,
        
        // Project errors
        PROJECT_NOT_FOUND: ExitCodes.NOT_FOUND,
        PROJECT_REGISTER_FAILED: ExitCodes.GENERAL_ERROR,
        
        // Config errors
        CONFIG_READ_FAILED: ExitCodes.CONFIG_ERROR,
        CONFIG_WRITE_FAILED: ExitCodes.CONFIG_ERROR,
        VALIDATION_ERROR: ExitCodes.CONFIG_ERROR,
      };
      
      return codeMap[code] ?? ExitCodes.GENERAL_ERROR;
    }
    ```
    
    Create **src/cli/output/error.test.ts** with tests for exit code mapping and error output:
    
    ```typescript
    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
    import chalk from 'chalk';
    import { ServiceError } from '../../../lib/services/types.js';
    import { ExitCodes, handleCLIError } from './error.js';
    
    // Mock console.error and process.exit
    vi.mock('console', () => ({
      error: vi.fn(),
    }));
    
    describe('ExitCodes', () => {
      it('SUCCESS equals 0', () => {
        expect(ExitCodes.SUCCESS).toBe(0);
      });
      
      it('NOT_FOUND equals 3', () => {
        expect(ExitCodes.NOT_FOUND).toBe(3);
      });
      
      it('CONFIG_ERROR equals 4', () => {
        expect(ExitCodes.CONFIG_ERROR).toBe(4);
      });
    });
    
    describe('handleCLIError', () => {
      const mockExit = vi.spyOn(process, 'exit').mockImplementation(() => undefined as never);
      const mockError = vi.spyOn(console, 'error').mockImplementation(() => {});
      
      beforeEach(() => {
        mockExit.mockClear();
        mockError.mockClear();
      });
      
      afterEach(() => {
        mockExit.mockRestore();
        mockError.mockRestore();
      });
      
      it('handles ServiceError with mapped exit code', () => {
        const error = new ServiceError('Template not found', 'TEMPLATE_NOT_FOUND');
        handleCLIError(error);
        
        expect(mockError).toHaveBeenCalled();
        expect(mockExit).toHaveBeenCalledWith(ExitCodes.NOT_FOUND);
      });
      
      it('handles generic Error with GENERAL_ERROR code', () => {
        const error = new Error('Something went wrong');
        handleCLIError(error);
        
        expect(mockError).toHaveBeenCalled();
        expect(mockExit).toHaveBeenCalledWith(ExitCodes.GENERAL_ERROR);
      });
      
      it('handles unknown error type', () => {
        handleCLIError('string error');
        
        expect(mockError).toHaveBeenCalled();
        expect(mockExit).toHaveBeenCalledWith(ExitCodes.GENERAL_ERROR);
      });
      
      it('allows override exit code', () => {
        const error = new ServiceError('Test error', 'TEMPLATE_NOT_FOUND');
        handleCLIError(error, ExitCodes.MISUSE);
        
        expect(mockExit).toHaveBeenCalledWith(ExitCodes.MISUSE);
      });
    });
    ```
    
    Per D-03: Uses chalk for red color on error messages, console.error for stderr, process.exit for exit codes.
    Per U1: Error messages include ServiceError.code for categorization.
  </action>
  <verify>
    <automated>vitest run src/cli/output/error.test.ts --passWithNoTests</automated>
  </verify>
  <done>
    - ExitCodes constants defined (SUCCESS=0, NOT_FOUND=3, CONFIG_ERROR=4)
    - handleCLIError outputs colored errors via chalk
    - ServiceError codes mapped to appropriate exit codes
    - Unit tests pass for exit code mapping and error handling
  </done>
</task>

</tasks>

<verification>
- [ ] cli-table3 dependency installed (check package.json)
- [ ] All test stub files created (7 files in src/cli/)
- [ ] Error module exports ExitCodes and handleCLIError
- [ ] Error tests pass with mocked console/process
- [ ] npm test passes (Wave 0 infrastructure complete)
</verification>

<success_criteria>
Wave 0 infrastructure ready for Wave 1 implementation:
- cli-table3 available for table formatting
- Test stubs enable TDD workflow
- Error handling module provides unified CLI error output
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli-interface/05-01-SUMMARY.md`
</output>
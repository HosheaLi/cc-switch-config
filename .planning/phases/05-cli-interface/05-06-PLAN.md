---
phase: 05-cli-interface
plan: 06
type: execute
wave: 4
depends_on: [05-02, 05-03, 05-04, 05-05]
files_modified: [src/index.ts, src/cli/output/index.ts]
autonomous: true
requirements: [D-08, M4]
user_setup: []
must_haves:
  truths:
    - "CLI entry point replaces skeleton in src/index.ts"
    - "Running cc-config command executes CLI interface"
    - "CLI module has barrel export for clean imports"
    - "No ink/react imports in CLI output utilities (M4 verified)"
    - "All CLI commands work when called from bin entry"
  artifacts:
    - path: "src/index.ts"
      provides: "Shebang entry + CLI import"
      min_lines: 15
      contains: "#!/usr/bin/env node"
    - path: "src/cli/output/index.ts"
      provides: "Barrel export for output utilities"
      exports: ["ExitCodes", "handleCLIError", "formatProjectTable"]
  key_links:
    - from: "src/index.ts"
      to: "src/cli/index.ts"
      via: "runCLI import"
      pattern: "import.*runCLI"
    - from: "src/cli/output/index.ts"
      to: "src/cli/output/error.ts"
      via: "Export aggregation"
      pattern: "export.*ExitCodes"
---

<objective>
完成 CLI 集成，替换 skeleton 入口，添加 barrel export，验证 M4 约束（CLI 无 UI 依赖）。

Purpose: src/index.ts 是 package.json bin 入口，必须包含 shebang 和 CLI import。M4 验证确保 CLI 层不依赖 ink/react（Phase 06 才引入 TUI）。
Output: 更新的 src/index.ts、CLI barrel export、M4 验证测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/CONSTITUTION.md
@.planning/phases/05-cli-interface/05-02-SUMMARY.md
@.planning/phases/05-cli-interface/05-03-SUMMARY.md
@.planning/phases/05-cli-interface/05-04-SUMMARY.md
@.planning/phases/05-cli-interface/05-05-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/cli/index.ts:
```typescript
export async function runCLI(argv?: string[]): Promise<void>;
```

From src/cli/output/error.ts:
```typescript
export const ExitCodes = { SUCCESS: 0, ... };
export function handleCLIError(error: unknown): void;
```

From src/cli/output/table.ts:
```typescript
export function formatProjectTable(projects: ProjectEntry[]): string;
```

From package.json:
```json
{
  "bin": {
    "cc-config": "./dist/index.js"
  },
  "dependencies": {
    "ink": "7.0.0",  // Only used in Phase 06 TUI
    "react": "19.2.5"
  }
}
```

From CONSTITUTION.md:
```markdown
## Code Standards
- **No UI in Services**: Services/Store/CLI/TUI modules must NOT import ink/react (M4 verification)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update src/index.ts with shebang and CLI entry import</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts (current skeleton)
    - src/cli/index.ts (runCLI function)
    - package.json (bin entry)
  </read_first>
  <behavior>
    - Test: src/index.ts contains shebang "#!/usr/bin/env node"
    - Test: src/index.ts imports runCLI from cli/index.js
    - Test: src/index.ts calls runCLI().catch(handleCLIError)
  </behavior>
  <action>
    Replace **src/index.ts** skeleton with CLI entry (per D-08):
    
    ```typescript
    /**
     * CLI Entry Point - Shebang + CLI Import
     *
     * Per D-08: src/cli/index.ts contains CLI logic, src/index.ts is shebang entry.
     * This file is the package.json bin entry (./dist/index.js).
     *
     * Responsibility:
     * - Shebang for executable Node.js script
     * - Import and call CLI runCLI function
     * - Handle top-level errors with handleCLIError
     */
    
    #!/usr/bin/env node
    import { runCLI } from './cli/index.js';
    import { handleCLIError } from './cli/output/error.js';
    
    // Launch CLI application
    runCLI().catch(handleCLIError);
    ```
    
    This replaces the Phase 1 skeleton:
    - Adds shebang for npm bin execution
    - Imports runCLI from CLI module
    - Catches all errors with handleCLIError (D-03)
    
    Per package.json: `bin: { "cc-config": "./dist/index.js" }`
    After tsup build, this file becomes the executable entry.
  </action>
  <verify>
    <automated>grep -E "^#!/usr/bin/env node|import.*runCLI" src/index.ts && echo "Entry point verified"</automated>
  </verify>
  <done>
    - src/index.ts contains shebang "#!/usr/bin/env node"
    - src/index.ts imports runCLI from ./cli/index.js
    - src/index.ts calls runCLI().catch(handleCLIError)
    - Skeleton placeholder replaced with CLI entry
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CLI output barrel export</name>
  <files>src/cli/output/index.ts, src/cli/output/index.test.ts</files>
  <read_first>
    - src/cli/output/error.ts (ExitCodes, handleCLIError)
    - src/cli/output/table.ts (formatProjectTable)
    - package.json (exports field)
  </read_first>
  <behavior>
    - Test: output/index.ts exports ExitCodes from error.ts
    - Test: output/index.ts exports handleCLIError from error.ts
    - Test: output/index.ts exports formatProjectTable from table.ts
  </behavior>
  <action>
    Create **src/cli/output/index.ts** barrel export:
    
    ```typescript
    /**
     * CLI Output Utilities - Barrel Export
     *
     * Per D-08: Clean import path for CLI output utilities.
     *
     * Usage:
     * ```typescript
     * import { ExitCodes, handleCLIError, formatProjectTable } from '../output/index.js';
     * ```
     */
    
    // Error handling utilities
    export { ExitCodes, handleCLIError } from './error.js';
    
    // Table formatting utilities
    export { formatProjectTable, truncatePath } from './table.js';
    ```
    
    Create **src/cli/output/index.test.ts** for barrel export verification:
    
    ```typescript
    import { describe, it, expect } from 'vitest';
    
    describe('CLI output barrel export', () => {
      it('exports ExitCodes', async () => {
        const module = await import('./index.js');
        expect(module.ExitCodes).toBeDefined();
        expect(module.ExitCodes.SUCCESS).toBe(0);
      });
      
      it('exports handleCLIError', async () => {
        const module = await import('./index.js');
        expect(module.handleCLIError).toBeDefined();
        expect(typeof module.handleCLIError).toBe('function');
      });
      
      it('exports formatProjectTable', async () => {
        const module = await import('./index.js');
        expect(module.formatProjectTable).toBeDefined();
        expect(typeof module.formatProjectTable).toBe('function');
      });
      
      it('exports truncatePath', async () => {
        const module = await import('./index.js');
        expect(module.truncatePath).toBeDefined();
        expect(typeof module.truncatePath).toBe('function');
      });
    });
    ```
    
    Per D-08: src/cli/output/ 目录有 barrel export 统一导出入口。
  </action>
  <verify>
    <automated>vitest run src/cli/output/index.test.ts --passWithNoTests</automated>
  </verify>
  <done>
    - output/index.ts exports ExitCodes and handleCLIError
    - output/index.ts exports formatProjectTable and truncatePath
    - Barrel export enables clean imports
    - Unit tests pass for barrel exports
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: M4 Verification - Ensure no ink/react imports in CLI</name>
  <files>src/cli/m4-verification.test.ts</files>
  <read_first>
    - .planning/CONSTITUTION.md (M4: No UI in Services/Store/CLI)
    - src/cli/output/error.ts (check imports)
    - src/cli/output/table.ts (check imports)
    - src/cli/commands/*.ts (check imports)
  </read_first>
  <behavior>
    - Test: src/cli/output/error.ts does NOT import ink
    - Test: src/cli/output/table.ts does NOT import ink
    - Test: src/cli/commands/*.ts do NOT import react
    - Test: All CLI files use chalk for coloring, not ink
  </behavior>
  <action>
    Create **src/cli/m4-verification.test.ts** for M4 constraint verification:
    
    ```typescript
    /**
     * M4 Verification Test - CLI Module No UI Dependencies
     *
     * Per CONSTITUTION.md M4: CLI module must NOT import ink/react.
     * CLI uses chalk for output formatting, ink is only for Phase 06 TUI.
     *
     * This test enforces the architectural boundary:
     * - CLI layer: Uses chalk for colored output, commander for commands
     * - TUI layer (Phase 06): Uses ink/react for interactive UI
     *
     * The separation ensures CLI remains testable and performant
     * without UI rendering overhead.
     */
    
    import { describe, it, expect } from 'vitest';
    import fs from 'fs-extra';
    import path from 'path';
    
    describe('M4 Verification: CLI no UI imports', () => {
      const cliDir = path.join(process.cwd(), 'src', 'cli');
      
      /**
       * Recursively find all TypeScript files in CLI directory.
       */
      async function findCLIFiles(dir: string): Promise<string[]> {
        const files: string[] = [];
        const entries = await fs.readdir(dir, { withFileTypes: true });
        
        for (const entry of entries) {
          const fullPath = path.join(dir, entry.name);
          if (entry.isDirectory()) {
            files.push(...await findCLIFiles(fullPath));
          } else if (entry.isFile() && entry.name.endsWith('.ts') && !entry.name.endsWith('.test.ts')) {
            files.push(fullPath);
          }
        }
        
        return files;
      }
      
      /**
       * Check if file contains forbidden imports.
       */
      async function checkForbiddenImports(filePath: string): Promise<string[]> {
        const content = await fs.readFile(filePath, 'utf-8');
        const forbidden: string[] = [];
        
        // Check for ink imports
        if (content.includes("from 'ink'") || content.includes('from "ink"')) {
          forbidden.push('ink');
        }
        
        // Check for react imports (allowed in Phase 06 TUI only)
        // CLI commands should NOT use react directly
        if (content.includes("from 'react'") || content.includes('from "react"')) {
          // Exception: TUI components (Phase 06) - but CLI commands shouldn't
          if (!filePath.includes('tui-launch.ts')) {
            forbidden.push('react');
          }
        }
        
        return forbidden;
      }
      
      it('CLI output utilities do not import ink', async () => {
        const outputDir = path.join(cliDir, 'output');
        const files = await findCLIFiles(outputDir);
        
        for (const file of files) {
          const forbidden = await checkForbiddenImports(file);
          expect(forbidden).toEqual([]);
        }
      });
      
      it('CLI commands do not import react', async () => {
        const commandsDir = path.join(cliDir, 'commands');
        const files = await findCLIFiles(commandsDir);
        
        for (const file of files) {
          const forbidden = await checkForbiddenImports(file);
          expect(forbidden).toEqual([]);
        }
      });
      
      it('CLI index does not import ink', async () => {
        const indexPath = path.join(cliDir, 'index.ts');
        const forbidden = await checkForbiddenImports(indexPath);
        expect(forbidden).toEqual([]);
      });
      
      it('All CLI files use chalk for coloring', async () => {
        const files = await findCLIFiles(cliDir);
        
        // Files that output colored text should import chalk
        const outputFiles = files.filter(f => 
          f.includes('output/') || f.includes('commands/')
        );
        
        for (const file of outputFiles) {
          const content = await fs.readFile(file, 'utf-8');
          // If file uses chalk (has colored output), verify import
          if (content.includes('chalk.green') || content.includes('chalk.red') || content.includes('chalk.yellow')) {
            expect(content.includes("from 'chalk'") || content.includes('from "chalk"')).toBe(true);
          }
        }
      });
    });
    ```
    
    Per CONSTITUTION.md M4: CLI 模块禁止导入 ink/react（TUI 专用）。
    此测试验证架构边界，确保 CLI 层保持简洁可测试。
  </action>
  <verify>
    <automated>vitest run src/cli/m4-verification.test.ts --passWithNoTests</automated>
  </verify>
  <done>
    - CLI output utilities verified (no ink imports)
    - CLI commands verified (no react imports)
    - CLI index verified (no UI imports)
    - M4 constraint enforced via automated test
  </done>
</task>

</tasks>

<verification>
- [ ] src/index.ts updated with shebang
- [ ] src/index.ts imports runCLI
- [ ] CLI output barrel export created
- [ ] M4 verification test created
- [ ] No ink imports in CLI output
- [ ] No react imports in CLI commands
- [ ] All tests pass
</verification>

<success_criteria>
CLI integration complete:
- `npm run build` produces dist/index.js with shebang
- `cc-config --version` works when installed
- `cc-config --help` shows command reference
- All 4 core commands accessible
- M4 verification ensures CLI is UI-independent
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli-interface/05-06-SUMMARY.md`
</output>
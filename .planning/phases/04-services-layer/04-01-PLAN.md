---
phase: 04-services-layer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [src/lib/services/types.ts, src/lib/services/types.test.ts, src/lib/services/config-service.test.ts, src/lib/services/project-service.test.ts, src/lib/services/template-service.test.ts, src/lib/services/provider-service.test.ts]
autonomous: true
requirements: [M4]
user_setup: []
must_haves:
  truths:
    - "ServiceError class exists and extends Error"
    - "ServiceError has code field for error categorization"
    - "All service test stubs exist and fail immediately"
    - "Test stubs import service modules (verify module paths)"
  artifacts:
    - path: "src/lib/services/types.ts"
      provides: "ServiceError class definition"
      exports: ["ServiceError"]
      min_lines: 20
    - path: "src/lib/services/types.test.ts"
      provides: "ServiceError unit tests"
      contains: "describe('ServiceError'"
    - path: "src/lib/services/config-service.test.ts"
      provides: "ConfigService test stub (Wave 0 placeholder)"
      contains: "describe('ConfigService'"
    - path: "src/lib/services/project-service.test.ts"
      provides: "ProjectService test stub"
      contains: "describe('ProjectService'"
    - path: "src/lib/services/template-service.test.ts"
      provides: "TemplateService test stub"
      contains: "describe('TemplateService'"
    - path: "src/lib/services/provider-service.test.ts"
      provides: "ProviderService test stub"
      contains: "describe('ProviderService'"
  key_links:
    - from: "src/lib/services/types.ts"
      to: "Error (native)"
      via: "extends Error"
      pattern: "class ServiceError extends Error"
---

<objective>
Create foundation for services layer: ServiceError class and Nyquist Wave 0 test stubs.

Purpose: Establish error handling pattern (D-02) and create test scaffolds that fail immediately, enabling TDD workflow in Wave 1.
Output: ServiceError class + all service test stubs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-services-layer/04-CONTEXT.md
@.planning/phases/04-services-layer/04-VALIDATION.md

<interfaces>
<!-- Key patterns from existing codebase -->

From src/lib/types/validation.ts (ValidationError pattern to follow):
```typescript
export class ValidationError extends Error {
  public readonly issues: z.core.$ZodIssue[];

  constructor(message: string, issues: z.core.$ZodIssue[]) {
    super(message);
    this.name = 'ValidationError';
    this.issues = issues;
  }

  getMessages(): string[] {
    return this.issues.map(issue => {
      const path = issue.path?.join('.') || 'root';
      return `${path}: ${issue.message}`;
    });
  }
}
```

From src/lib/store/state.ts (AppStateData needs scanDirectories extension per D-05):
```typescript
export interface AppStateData {
  activeProjectId: string | null;
  lastUsedTemplate: string | null;
  uiPreferences: { theme: 'dark' | 'light'; showPreview: boolean };
  recentProjects: string[];
  // D-05: Add scanDirectories: string[]
}
```

Test pattern from src/lib/store/*.test.ts (temp directory isolation):
```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import fs from 'fs-extra';
import path from 'path';
import os from 'os';

describe('ServiceName', () => {
  let tempDir: string;

  beforeEach(async () => {
    tempDir = await fs.mkdtemp(path.join(os.tmpdir(), 'service-test-'));
  });

  afterEach(async () => {
    await fs.remove(tempDir);
  });

  it.todo('test description'); // Wave 0 stub pattern
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ServiceError class</name>
  <files>src/lib/services/types.ts, src/lib/services/types.test.ts</files>
  <read_first>
    - src/lib/types/validation.ts (ValidationError pattern to follow)
    - src/lib/store/index.ts (barrel export pattern)
  </read_first>
  <behavior>
    - Test 1: ServiceError extends Error
    - Test 2: ServiceError has code property (string)
    - Test 3: ServiceError constructor accepts (message: string, code: string)
    - Test 4: ServiceError.name is 'ServiceError'
  </behavior>
  <action>
    Per D-02: Services throw Error, caller handles. Create ServiceError class following ValidationError pattern from validation.ts:

    1. Create src/lib/services/types.ts:
       - Export ServiceError class extending Error
       - Constructor: (message: string, code: string)
       - Set this.name = 'ServiceError'
       - Add public readonly code: string property
       - Optional: Add context?: Record<string, unknown> for additional error data

    2. Create src/lib/services/types.test.ts:
       - Test ServiceError extends Error
       - Test code property is set correctly
       - Test name is 'ServiceError'
       - Test instanceof Error works

    Run tests after implementation: `npm test -- src/lib/services/types.test.ts`
  </action>
  <verify>
    <automated>npm test -- src/lib/services/types.test.ts</automated>
  </verify>
  <done>
    ServiceError class exported from types.ts
    types.test.ts passes all tests
    ServiceError extends Error with code property
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test stubs for all services</name>
  <files>src/lib/services/config-service.test.ts, src/lib/services/project-service.test.ts, src/lib/services/template-service.test.ts, src/lib/services/provider-service.test.ts</files>
  <read_first>
    - src/lib/store/config.test.ts (test pattern to follow)
    - src/lib/store/template.test.ts (test pattern to follow)
  </read_first>
  <action>
    Per Nyquist validation strategy (04-VALIDATION.md Wave 0 Requirements): Create failing test stubs that import service modules.

    Each test file:
    1. Import vitest: describe, it, expect, beforeEach, afterEach
    2. Import fs-extra, path, os for temp directory setup
    3. beforeEach: create tempDir via fs.mkdtemp
    4. afterEach: remove tempDir via fs.remove
    5. Top-level describe block with service name
    6. Multiple it.todo() stubs with descriptive names (will fail immediately)

    Test stub content for each file:

    config-service.test.ts stubs:
    - it.todo('readConfig loads project config')
    - it.todo('writeConfig saves config with validation')
    - it.todo('mergeConfigs combines project and template')
    - it.todo('validateConfig checks schema')

    project-service.test.ts stubs:
    - it.todo('scanProjects finds .claude directories')
    - it.todo('registerProject creates project entry')
    - it.todo('listProjects returns all registered')
    - it.todo('updateProject modifies activeConfig')

    template-service.test.ts stubs:
    - it.todo('createTemplate saves template config')
    - it.todo('getTemplate retrieves by name')
    - it.todo('applyTemplate merges with project config')
    - it.todo('deleteTemplate removes from store')

    provider-service.test.ts stubs:
    - it.todo('testConnectivity checks endpoint reachable')
    - it.todo('testConnectivity handles timeout')
    - it.todo('testConnectivity returns latency')

    Note: Do NOT import actual service classes yet (they don't exist). Use placeholder imports that will fail, triggering Wave 1 implementation.

    Create services directory first: mkdir -p src/lib/services
  </action>
  <verify>
    <automated>ls src/lib/services/*.test.ts | wc -l && echo "expected 4 test stub files"</automated>
  </verify>
  <done>
    All 4 test stub files exist in src/lib/services/
    Each file has describe block and it.todo() stubs
    Test files follow temp directory isolation pattern
  </done>
</task>

</tasks>

<verification>
Run all tests to verify Wave 0 foundation:
```bash
npm test -- src/lib/services/types.test.ts  # Should pass (ServiceError implemented)
npm test -- src/lib/services/*.test.ts      # types.test.ts passes, others have todo stubs
```

Verify file structure:
```bash
ls -la src/lib/services/
# Should show: types.ts, types.test.ts, config-service.test.ts, project-service.test.ts, template-service.test.ts, provider-service.test.ts
```
</verification>

<success_criteria>
- ServiceError class exists with code property
- ServiceError tests pass
- All 4 service test stub files created
- Test stubs use it.todo() pattern (fail immediately)
- Services directory created
</success_criteria>

<output>
After completion, create `.planning/phases/04-services-layer/04-01-SUMMARY.md`
</output>
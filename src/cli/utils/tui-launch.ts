/**
 * TUI Launch Utility
 *
 * Placeholder for Phase 06 TUI implementation.
 * Per D-02: No args launches TUI, args → CLI commands.
 *
 * This stub will be replaced in Phase 06 with ink-based TUI.
 */

/**
 * Launch the interactive TUI interface.
 * Per D-02: Called when CLI receives no arguments.
 *
 * @returns Promise that resolves when TUI exits
 */
export async function launchTUI(): Promise<void> {
  // Phase 06 stub: TUI not yet implemented
  console.log('TUI not yet implemented. Use --help for available commands.');
}